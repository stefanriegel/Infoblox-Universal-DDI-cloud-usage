"""GCP cloud provider for resource discovery and token estimation.

Implements GCP-specific authentication, project enumeration,
client factory, and discovery provider that integrate with the
Phase 1 core infrastructure (AuthValidator, DiscoveryProvider ABCs).
"""
