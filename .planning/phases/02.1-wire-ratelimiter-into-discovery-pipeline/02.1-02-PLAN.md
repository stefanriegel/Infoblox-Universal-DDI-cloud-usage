---
phase: 02.1-wire-ratelimiter-into-discovery-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cloud_usage/counting/ip_counter.py
  - tests/test_ip_counter.py
autonomous: true
requirements:
  - DISC-05

must_haves:
  truths:
    - "The orphaned count_ips() function no longer exists in ip_counter.py"
    - "No test references to count_ips remain in the test suite"
    - "deduplicate_ips_per_vpc() still works correctly (active code preserved)"
    - "All remaining tests pass after dead code removal"
  artifacts:
    - path: "src/cloud_usage/counting/ip_counter.py"
      provides: "deduplicate_ips_per_vpc() only -- count_ips() removed"
      contains: "deduplicate_ips_per_vpc"
    - path: "tests/test_ip_counter.py"
      provides: "Tests for deduplicate_ips_per_vpc only -- TestCountIps class removed"
      contains: "TestDeduplicateIpsPerVpc"
  key_links:
    - from: "src/cloud_usage/cli.py"
      to: "src/cloud_usage/counting/ip_counter.py"
      via: "import deduplicate_ips_per_vpc (still works after removal of count_ips)"
      pattern: "deduplicate_ips_per_vpc"
---

<objective>
Remove orphaned count_ips() function from ip_counter.py and its test class, completing the dead code cleanup portion of the v1.0 audit gap closure.

Purpose: count_ips() was superseded by deduplicate_ips_per_vpc() which provides per-VPC IP space deduplication. The old function is not imported by any production code (cli.py, orchestrator, providers) -- only by test_ip_counter.py.

Output: Cleaned ip_counter.py with only deduplicate_ips_per_vpc(), and test file without the TestCountIps class.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-wire-ratelimiter-into-discovery-pipeline/02.1-CONTEXT.md
@.planning/phases/02.1-wire-ratelimiter-into-discovery-pipeline/02.1-RESEARCH.md
@src/cloud_usage/counting/ip_counter.py
@tests/test_ip_counter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove count_ips() and its tests</name>
  <files>
    src/cloud_usage/counting/ip_counter.py
    tests/test_ip_counter.py
  </files>
  <action>
**ip_counter.py changes:**

1. Remove the entire `count_ips()` function (lines 16-52 in current file). This includes the function definition, docstring, and all implementation.

2. Remove the `import ipaddress` import ONLY if it is no longer used by `deduplicate_ips_per_vpc()`. Check: `deduplicate_ips_per_vpc()` does NOT use `ipaddress` -- it uses tuple dedup keys, not IP classification. So `import ipaddress` should be removed.

3. Keep the module docstring, `from __future__ import annotations`, `from collections import defaultdict`, `from cloud_usage.schema.resource import CloudResource`, and the entire `deduplicate_ips_per_vpc()` function.

4. Update the module docstring to reflect that only deduplication remains -- remove mention of "IP extraction" if it refers to count_ips behavior. The docstring should say something like:

```python
"""
Per-VPC IP de-duplication for cloud resource counting.

De-duplicates IP addresses per VPC IP space and provides per-account
breakdowns for token calculation.
"""
```

**test_ip_counter.py changes:**

1. Remove the `count_ips` import from line 16 (keep `deduplicate_ips_per_vpc` import only).

2. Remove the entire `TestCountIps` class (approximately lines 45-109 in current file -- 7 test methods: test_empty_resources, test_private_ips, test_public_ips, test_ipv6_private, test_mixed_ips, test_skips_uncounted, test_invalid_ip_skipped, test_ipv6_public).

3. Keep the `TestDeduplicateIpsPerVpc` class and all its tests intact.

4. Verify: run `grep -n count_ips tests/test_ip_counter.py` should return no matches after changes.

**Verification checks before removing:**
- `grep -rn "from cloud_usage.counting.ip_counter import count_ips" src/` should return 0 matches (confirming no production code uses it)
- `grep -rn "count_ips" src/cloud_usage/cli.py` should return 0 matches for the function name (the variable `ip_counts` is different and should NOT be confused with `count_ips`)
- `src/cloud_usage/counting/__init__.py` has no re-exports -- no cleanup needed there
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_ip_counter.py tests/test_integration_aws.py -x -v && grep -c "count_ips" src/cloud_usage/counting/ip_counter.py; test $? -ne 0 || echo "FAIL: count_ips still present"</automated>
  </verify>
  <done>
    - count_ips() function is removed from ip_counter.py
    - TestCountIps class is removed from test_ip_counter.py
    - import ipaddress is removed from ip_counter.py (unused by remaining function)
    - count_ips import is removed from test_ip_counter.py
    - deduplicate_ips_per_vpc() still works correctly (all its tests pass)
    - No production code is broken (count_ips was only used by tests)
    - Integration tests still pass (they use deduplicate_ips_per_vpc, not count_ips)
  </done>
</task>

</tasks>

<verification>
Run the full test suite to confirm nothing is broken:

```bash
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/ -x -v
```

Verify dead code is gone:
```bash
grep -rn "count_ips" src/cloud_usage/counting/ tests/test_ip_counter.py
```
Should return 0 matches.
</verification>

<success_criteria>
- count_ips() function no longer exists in ip_counter.py
- No test or production code references count_ips
- deduplicate_ips_per_vpc() tests all pass
- Integration tests all pass
- Module docstring updated to reflect current scope
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-wire-ratelimiter-into-discovery-pipeline/02.1-02-SUMMARY.md`
</output>
