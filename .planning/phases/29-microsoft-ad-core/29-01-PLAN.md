---
phase: 29-microsoft-ad-core
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_ad_collector.py
  - tests/test_ad_runner.py
  - tests/test_categorizer.py
autonomous: true
requirements:
  - AD-01
  - AD-02
  - AD-03
  - AD-04
  - AD-05
  - AD-06
  - AD-07
  - AD-08

must_haves:
  truths:
    - "Failing tests exist that import from src/cloud_usage/providers/ad/collector.py (ImportError confirms RED)"
    - "Failing tests exist for DNS zone/record collection (AD-02)"
    - "Failing tests exist for DHCP scope/lease/reservation collection (AD-03)"
    - "Failing tests exist for AD User collection with SID dedup (AD-04)"
    - "Failing tests exist for autodiscovery (AD-07)"
    - "Categorizer test asserts ad-dns-zone, ad-dns-record, ad-dhcp-scope, ad-user in DDI_TYPES (currently fails)"
  artifacts:
    - path: "tests/test_ad_collector.py"
      provides: "RED tests for collector, options, autodiscovery, auth modes"
    - path: "tests/test_ad_runner.py"
      provides: "RED tests for run_ad_analysis() and XLS output (AD-08)"
    - path: "tests/test_categorizer.py"
      provides: "Updated with AD DDI type assertions"
  key_links:
    - from: "tests/test_ad_collector.py"
      to: "src/cloud_usage/providers/ad/collector.py"
      via: "import — triggers ImportError RED state"
      pattern: "from cloud_usage.providers.ad.collector import"
    - from: "tests/test_ad_runner.py"
      to: "src/cloud_usage/providers/ad/__init__.py"
      via: "import — triggers ImportError RED state"
      pattern: "from cloud_usage.providers.ad import run_ad_analysis"
---

<objective>
Write failing tests for the Microsoft AD provider before any implementation exists. Confirms RED state via ImportError. Tests cover all 8 AD requirements so each can turn GREEN during implementation plans.

Purpose: TDD RED gate — define the contract before writing code. Every import fails until 29-02/29-03 create the modules.
Output: Two new test files and updated test_categorizer.py, all failing.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-microsoft-ad-core/29-CONTEXT.md

<interfaces>
<!-- Key existing types/functions the new tests will reference -->

From src/cloud_usage/schema/resource.py:
```python
@dataclass
class CloudResource:
    resource_id: str
    resource_type: str
    provider: str
    account_id: str
    region: str
    name: str
    ip_addresses: list[str] = field(default_factory=list)
    tags: dict[str, str] = field(default_factory=dict)
    details: dict[str, Any] = field(default_factory=dict)
    discovered_at: str = ...
    counted: bool | None = None
    category: str | None = None
    skip_reason: str | None = None
```

From src/cloud_usage/counting/categorizer.py:
```python
DDI_TYPES: set[str]  # currently has no ad-* entries
```

Reference (do_not_commit/…/microsoft_ad.py) SUPPORTED_DNS_RECORD_TYPES (13 types):
  A, AAAA, CNAME, MX, TXT, CAA, SRV, SVCB, HTTPS, PTR, NS, SOA, NAPTR

Planned module layout (to create in 29-02):
  src/cloud_usage/providers/ad/__init__.py  → exports run_ad_analysis
  src/cloud_usage/providers/ad/collector.py → MicrosoftAdCollector class
  src/cloud_usage/providers/ad/options.py   → AdOptions dataclass + helpers

Planned resource_type strings (Claude's discretion — carry forward into 29-02/03):
  "ad-dns-zone"     — one per DNS zone (DDI)
  "ad-dns-record"   — one per supported DNS record after dedup (DDI)
  "ad-dhcp-scope"   — one per DHCP scope (DDI)
  "ad-user"         — one per AD User after SID dedup (Asset via ip_addresses trick or asset category)
</interfaces>
</context>

<feature>
  <name>Microsoft AD RED gate tests</name>
  <files>tests/test_ad_collector.py, tests/test_ad_runner.py, tests/test_categorizer.py (append)</files>
  <behavior>
    test_ad_collector.py:

    - TestAdCollectorImport: import MicrosoftAdCollector from cloud_usage.providers.ad.collector → ImportError
    - TestAdOptionsImport: import AdOptions, normalize_ad_servers, normalize_ad_services from cloud_usage.providers.ad.options → ImportError
    - TestCollectDns: MicrosoftAdCollector._collect_dns() with mocked session returning 2 zones (corp.example.com, test.corp.example.com) and 3 A records per zone → returns dict with zone_keys (len 2), supported_record_entries, ip_addresses extracted from A/AAAA data
    - TestCollectDhcp: _collect_dhcp() with mocked session returning 2 scopes (192.168.1.0, 192.168.2.0), 5 leases each, 2 reservations each → scope_keys len 2, lease_keys len 10, reservation_keys len 4
    - TestCollectUsers: _collect_users() with mocked session returning 3 users with SIDs → user_keys len 3 (SID-keyed)
    - TestCollectUsersSidDedup: _collect_users() where same SID appears twice → user_keys len 1 (deduped)
    - TestAutodiscover: _resolve_targets() with autodiscover=True, mocked session returning domains ["corp.example.com"] and DCs ["dc1.corp.example.com"] → targets includes dc1 with dns+user services
    - TestNtlmRequiresCredentials: AdOptions with auth_mode="ntlm" and no username/password → raises ValueError
    - TestKerberosNoCredentialsRequired: AdOptions with auth_mode="kerberos" and no credentials → succeeds
    - TestNormalizeAdServices: normalize_ad_services("dns,dhcp") → ("dns", "dhcp"); normalize_ad_services("") → raises ValueError; normalize_ad_services("bad") → raises ValueError
    - TestNormalizeAdServers: normalize_ad_servers("DC1.CORP.EXAMPLE.COM,dc2.corp.example.com") → ["dc1.corp.example.com", "dc2.corp.example.com"] (lowercase, deduped)
    - TestDcFailureContinue: collector._collect_server() raises WinRMError for dc1 but dc2 succeeds → result has dc1 with status="error", dc2 with status="ok"
    - TestAllDcsFail: run_ad_analysis() with all DCs failing → returns empty list and non-empty errors list

    test_ad_runner.py:

    - TestRunAdAnalysisImport: import run_ad_analysis from cloud_usage.providers.ad → ImportError
    - TestRunAdAnalysisReturnsCloudResources: run_ad_analysis() with mocked collector returning 2 zones + 5 records + 3 scopes + 10 users → returns list of CloudResource objects with correct resource_types (ad-dns-zone, ad-dns-record, ad-dhcp-scope, ad-user)
    - TestAdDnsZoneResourceType: dns zone CloudResource has provider="ad", resource_type="ad-dns-zone", ip_addresses=[]
    - TestAdDnsRecordResourceType: dns record CloudResource has resource_type="ad-dns-record"; A/AAAA records have ip_addresses populated with the record data IP
    - TestAdDhcpScopeResourceType: scope CloudResource has resource_type="ad-dhcp-scope", ip_addresses=[] (DDI-only)
    - TestAdDhcpIpCount: lease/reservation IPs are represented as separate CloudResource objects with resource_type="ad-dhcp-ip" and provider="ad" carrying ip_addresses=[ip_str] — OR alternatively aggregated into scope CloudResource ip_addresses list (test asserts the total IP count is correct regardless of shape)
    - TestAdUserResourceType: user CloudResource has resource_type="ad-user", ip_addresses=["0.0.0.0"] (sentinel to force asset categorization), details={"sid": "..."}
    - TestXlsReportProduced: run_ad_analysis() with output_path → calls write_xlsx_report with provider="ad" (mock assert)

    test_categorizer.py (append new test class):

    - TestAdDdiTypesInCategorizer: assert "ad-dns-zone" in DDI_TYPES → currently fails (RED); same for "ad-dns-record", "ad-dhcp-scope"
    - TestAdUserNotInDdiTypes: "ad-user" not in DDI_TYPES (users are Assets, not DDI)
  </behavior>
  <implementation>
    Write test files only. No production code. Confirm RED state with:
    pytest tests/test_ad_collector.py tests/test_ad_runner.py -x 2>&1 | head -20
    Expected: ImportError or ModuleNotFoundError on first import.

    For test_categorizer.py: append TestAdDdiTypesInCategorizer class.
    Expected: AssertionError because DDI_TYPES has no ad-* entries yet.

    RED commit: test(29-01): add failing AD provider tests (RED gate)
  </implementation>
</feature>

<verification>
pytest tests/test_ad_collector.py tests/test_ad_runner.py -x 2>&1 | grep -E "ERROR|ImportError|ModuleNotFoundError" | head -5

Expected: at least one ImportError or ModuleNotFoundError visible — confirms RED state.

pytest tests/test_categorizer.py -k "TestAdDdi" -x 2>&1 | grep -E "FAILED|AssertionError" | head -3

Expected: FAILED with AssertionError — ad-dns-zone not in DDI_TYPES yet.
</verification>

<success_criteria>
- tests/test_ad_collector.py exists with 13+ test methods
- tests/test_ad_runner.py exists with 8+ test methods
- test_categorizer.py has TestAdDdiTypesInCategorizer class
- Running pytest on the new files produces ImportError or AssertionError (RED), not syntax errors
- No production code created in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/29-microsoft-ad-core/29-01-SUMMARY.md`
</output>
