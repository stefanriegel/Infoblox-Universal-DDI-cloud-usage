---
phase: 07-integration-gap-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - tests/test_rate_limiter.py
  - tests/test_orchestrator.py
  - tests/test_cli.py
autonomous: true
requirements:
  - DISC-05
  - RESIL-01

must_haves:
  truths:
    - "Unit tests for record_success() verify immediate reset to delay=0.0 and consecutive_rate_limits=0 on a single call"
    - "Integration test proves orchestrator calls record_success() on successful discover_account() and does NOT call it on failure"
    - "Integration test proves CLI _get_discovery_providers() passes checkpoint_engine to AzureDiscoveryProvider and GCPDiscoveryProvider"
    - "No test references _DECAY_FACTOR or _MIN_DELAY constants"
    - "Full test suite passes with >= 977 tests (pre-existing baseline)"
  artifacts:
    - path: "tests/test_rate_limiter.py"
      provides: "Updated tests for immediate-reset record_success() semantics"
      contains: "record_success"
    - path: "tests/test_orchestrator.py"
      provides: "Integration tests for record_success() call in orchestrator"
      contains: "record_success"
    - path: "tests/test_cli.py"
      provides: "Integration tests for checkpoint_engine passed to providers"
      contains: "checkpoint_engine"
  key_links:
    - from: "tests/test_orchestrator.py"
      to: "src/cloud_usage/discovery/orchestrator.py"
      via: "mock_rate_limiter.record_success.assert_called with provider name"
      pattern: "record_success.*assert_called"
    - from: "tests/test_cli.py"
      to: "src/cloud_usage/cli.py"
      via: "mock provider constructor asserts checkpoint_engine kwarg"
      pattern: "checkpoint_engine.*mock"
---

<objective>
Update existing tests for immediate-reset semantics, remove dead code references, and add new integration tests proving record_success() and checkpoint_engine wiring works.

Purpose: Per CONTEXT.md locked decision, dead code cleanup includes production AND test code, with new integration tests proving the wiring. Separate commit from wiring changes for easier review.

Output: Updated test files with correct semantics and new integration tests; full suite passes.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-gap-closure/07-CONTEXT.md
@.planning/phases/07-integration-gap-closure/07-RESEARCH.md
@.planning/phases/07-integration-gap-closure/07-01-SUMMARY.md
@src/cloud_usage/resilience/rate_limiter.py
@src/cloud_usage/discovery/orchestrator.py
@src/cloud_usage/cli.py
@tests/test_rate_limiter.py
@tests/test_orchestrator.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update rate limiter tests for immediate-reset semantics and remove dead constant references</name>
  <files>
    tests/test_rate_limiter.py
  </files>
  <action>
**Update 3 existing tests in tests/test_rate_limiter.py that rely on gradual decay behavior:**

1. Find `test_success_decreases_delay` (or similarly named test verifying record_success reduces delay). Update to assert that a single record_success() call sets delay to exactly 0.0 (not just "less than before"). The test should:
   - Call record_rate_limit() to create non-zero backoff state
   - Call record_success() once
   - Assert get_delay() returns 0.0

2. Find `test_multiple_successes_return_to_zero` (or similarly named test verifying multiple successes needed to reach zero). Update to assert that only ONE record_success() call is needed to reach zero, not multiple. The test should:
   - Call record_rate_limit() to create non-zero backoff state
   - Call record_success() exactly once
   - Assert get_delay() returns 0.0
   - Assert that record_success() is idempotent (calling again on already-zero state keeps it at zero)

3. Find `test_success_reduces_throttle_state` (or similarly named test). Update to verify that consecutive_rate_limits resets to 0 immediately (not decremented by 1). The test should:
   - Call record_rate_limit() multiple times to set consecutive_rate_limits > 1
   - Call record_success() once
   - Assert get_delay() returns 0.0
   - Call is_throttled() or get_delay() to confirm backoff is fully cleared

4. Remove any references to `_DECAY_FACTOR` or `_MIN_DELAY` constants from test imports or test assertions. These constants no longer exist in the production code.

5. If any test imports these constants directly (e.g., `from cloud_usage.resilience.rate_limiter import _DECAY_FACTOR`), remove those imports.

**Important:** Do NOT remove the test methods themselves — update their assertions to match the new immediate-reset semantics. The tests are valid; only the expected behavior changed.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python3 -m pytest tests/test_rate_limiter.py -x --tb=short -q 2>&1 | tail -20</automated>
    <manual>Verify no test references _DECAY_FACTOR or _MIN_DELAY</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>All rate limiter tests pass with immediate-reset semantics; no references to removed constants; single record_success() call verified to reset delay to 0.0 and consecutive_rate_limits to 0</done>
</task>

<task type="auto">
  <name>Task 2: Add integration tests for record_success() wiring and checkpoint_engine threading</name>
  <files>
    tests/test_orchestrator.py
    tests/test_cli.py
  </files>
  <action>
**tests/test_orchestrator.py — Add 2 new tests for record_success() integration:**

1. Add test `test_discover_account_calls_record_success_on_success`:
   - Create mock RateLimiter with `get_delay.return_value = 0.0`
   - Create mock provider that returns resources from discover_account() (no exception)
   - Run orchestrator (use existing test helper pattern from test file)
   - Assert `mock_rate_limiter.record_success.assert_called_with(provider_name)` where provider_name matches the mock provider's provider_name
   - This proves the wiring from Plan 07-01 Task 1 works

2. Add test `test_discover_account_does_not_call_record_success_on_failure`:
   - Create mock RateLimiter with `get_delay.return_value = 0.0`
   - Create mock provider whose discover_account() raises an exception
   - Run orchestrator
   - Assert `mock_rate_limiter.record_success.assert_not_called()`
   - This proves record_success() is only called on the success path

Follow existing test patterns in the file: use `_make_orchestrator` helper if available, or create mocks matching the existing mock setup pattern (MagicMock with spec=RateLimiter, etc.).

**tests/test_cli.py — Add 2 new tests for checkpoint_engine threading:**

1. Add test `test_get_discovery_providers_passes_checkpoint_to_azure`:
   - Mock AzureDiscoveryProvider class import in cli module
   - Mock the Azure auth and subscription enumeration to return valid values
   - Create a mock checkpoint_engine object
   - Call `_get_discovery_providers(["azure"], args, checkpoint_engine)` with appropriate args namespace
   - Assert the AzureDiscoveryProvider constructor was called with `checkpoint_engine=mock_checkpoint`

2. Add test `test_get_discovery_providers_passes_checkpoint_to_gcp`:
   - Mock GCPDiscoveryProvider class import in cli module
   - Mock the GCP auth and project enumeration to return valid values
   - Create a mock checkpoint_engine object
   - Call `_get_discovery_providers(["gcp"], args, checkpoint_engine)` with appropriate args namespace
   - Assert the GCPDiscoveryProvider constructor was called with `checkpoint_engine=mock_checkpoint`

Follow existing test patterns in test_cli.py for mocking provider construction. Use `unittest.mock.patch` to patch the provider classes at their import location in cli.py.

**Note on pre-existing test failures:** The research notes 8 pre-existing failures in test_cli.py and test_integration_aws.py. These are NOT caused by Phase 7 changes. After adding new tests, the total passing count should be >= 977 + (number of new tests added).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python3 -m pytest tests/test_orchestrator.py tests/test_cli.py tests/test_rate_limiter.py -x --tb=short -q 2>&1 | tail -20</automated>
    <manual>Verify new test names appear in test output: test_discover_account_calls_record_success_on_success, test_discover_account_does_not_call_record_success_on_failure, test_get_discovery_providers_passes_checkpoint_to_azure, test_get_discovery_providers_passes_checkpoint_to_gcp</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>4 new integration tests added and passing; 2 proving record_success() is called on success (not on failure); 2 proving checkpoint_engine flows through to Azure and GCP providers; full test suite passes with >= 981 tests (977 baseline + 4 new)</done>
</task>

</tasks>

<verification>
1. `python3 -m pytest tests/test_rate_limiter.py -v --tb=short -q` — all tests pass, immediate-reset semantics verified
2. `python3 -m pytest tests/test_orchestrator.py -v -k "record_success" --tb=short` — new orchestrator tests pass
3. `python3 -m pytest tests/test_cli.py -v -k "checkpoint" --tb=short` — new CLI tests pass
4. `grep -r "_DECAY_FACTOR\|_MIN_DELAY" tests/` — no matches (dead constant references removed)
5. `python3 -m pytest tests/ -x --tb=short -q` — total passing >= 981 (977 baseline + 4 new), no new failures
</verification>

<success_criteria>
- All 3 updated rate limiter tests pass with immediate-reset assertions
- 2 new orchestrator tests prove record_success() called on success, not on failure
- 2 new CLI tests prove checkpoint_engine passed to Azure and GCP providers
- No references to _DECAY_FACTOR or _MIN_DELAY in test code
- Full test suite: >= 981 passing, pre-existing 8 failures unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-gap-closure/07-02-SUMMARY.md`
</output>
