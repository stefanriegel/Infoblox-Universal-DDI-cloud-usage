---
phase: 36-calculator-visual-redesign
plan: 03
type: execute
wave: 3
depends_on:
  - 36-02
files_modified:
  - src/cloud_usage/dashboard/templates/partials/nios/complete.html
  - src/cloud_usage/dashboard/templates/partials/ad/complete.html
  - src/cloud_usage/dashboard/templates/pages/summary.html
  - src/cloud_usage/dashboard/static/app.css
autonomous: true
requirements:
  - DESIGN-05

must_haves:
  truths:
    - "NIOS complete partial wraps its content in an article.completion-card with section dividers"
    - "AD complete partial wraps its content in an article.completion-card with section dividers"
    - "Cloud summary template has a Token Summary section header before the summary_cards include"
    - "app.css defines .completion-card and .section-header/.section-divider classes"
  artifacts:
    - path: "src/cloud_usage/dashboard/templates/partials/nios/complete.html"
      provides: "single-card wrapper with hr dividers"
      contains: "completion-card"
    - path: "src/cloud_usage/dashboard/templates/partials/ad/complete.html"
      provides: "single-card wrapper with hr dividers"
      contains: "completion-card"
    - path: "src/cloud_usage/dashboard/templates/pages/summary.html"
      provides: "section headers with dividers between token summary, provider breakdown, account attribution"
      contains: "Token Summary"
    - path: "src/cloud_usage/dashboard/static/app.css"
      provides: ".completion-card card style, .section-header and .section-divider utility classes"
      contains: "completion-card"
  key_links:
    - from: "partials/nios/complete.html"
      to: "app.css .completion-card"
      via: "article element class attribute"
      pattern: "class=\"completion-card\""
    - from: "pages/summary.html"
      to: "app.css .section-header / .section-divider"
      via: "h3 and hr element class attributes"
      pattern: "class=\"section-header\""
---

<objective>
Implement card-based layouts on results/complete screens (DESIGN-05).

NIOS and AD complete partials get a single unified `.completion-card` wrapper with `<hr>` section dividers. The Cloud Summary tab gets labeled section headers (`Token Summary`, `Per-Provider Breakdown`, `Account Attribution`) with horizontal dividers for visual hierarchy. All CSS for these new classes is added to `app.css`.

Purpose: Results/complete screens feel polished and scannable — key metrics (token totals, breakdown tables) are visually separated without blending the data-heavy audit content with calculator-specific accent colors (kept neutral per locked decision).
Output: Three template edits + one CSS addition; all 11 xfail tests in test_dashboard_visual_redesign.py turn green.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-calculator-visual-redesign/36-RESEARCH.md

<interfaces>
<!-- NIOS/AD complete partials current structure (from research) -->
<!-- Both use: <article class="completion-banner"> as their outer wrapper -->
<!-- .completion-banner in app.css adds: border-left: 4px solid var(--ib-green); box-shadow -->
<!-- Phase 36: replace completion-banner class with completion-card on outer article -->
<!-- Do NOT nest cards — replace the class, restructure sections with <hr> dividers -->

<!-- Cloud summary.html current structure (from research) -->
<!-- Line 10: {% include "partials/summary_cards.html" %} — no preceding header -->
<!-- Line 13: <h3>Per-Provider Breakdown</h3> — already exists -->
<!-- Line 43: <h3>Per-Account Breakdown</h3> — already exists -->
<!-- Line 196: <h3>Download Reports</h3> — already exists -->
<!-- Phase 36: add <h3 class="section-header">Token Summary</h3> + <hr class="section-divider"> before include -->
<!-- Also add <hr class="section-divider"> before each existing <h3> section -->
<!-- Rename "Per-Account Breakdown" to "Account Attribution" per CONTEXT.md spec -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add .completion-card CSS class and section header/divider utilities to app.css</name>
  <files>src/cloud_usage/dashboard/static/app.css</files>
  <action>
Append three new CSS rule blocks to `app.css`. Place them in a clearly labeled section after the existing completion/results styles (search for `.completion-banner` — add the new rules after it).

**A. `.completion-card` — neutral card wrapper for complete screens:**
```css
.completion-card {
    background: #fff;
    border: 1px solid var(--ib-gray-200);
    border-radius: 8px;
    padding: 1.5rem;
    box-shadow: 0 1px 6px rgba(0, 0, 0, 0.08);
    margin-bottom: 1rem;
}

.completion-card hr {
    border: none;
    border-top: 1px solid var(--ib-gray-200);
    margin: 1.25rem 0;
}
```

No colored left border — this is intentionally neutral. The `.completion-banner` CSS rule can remain in app.css (no orphan harm since the HTML partials will no longer reference it). Do NOT delete the `.completion-banner` CSS rule.

**B. `.section-header` — small uppercase label for Cloud Summary sections:**
```css
.section-header {
    font-size: 0.875rem;
    font-weight: 600;
    color: var(--ib-gray-600);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-bottom: 0.75rem;
    margin-top: 0;
}
```

**C. `.section-divider` — lightweight horizontal separator:**
```css
.section-divider {
    border: none;
    border-top: 1px solid var(--ib-gray-200);
    margin: 1.5rem 0;
}
```

Do not modify any existing rules in this task. Append only.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && grep -c "completion-card\|section-header\|section-divider" src/cloud_usage/dashboard/static/app.css</automated>
  </verify>
  <done>grep returns at least 3 (one occurrence each of .completion-card, .section-header, .section-divider). app.css is parseable CSS (no syntax errors detectable by loading the app).</done>
</task>

<task type="auto">
  <name>Task 2: Restructure NIOS and AD complete partials with completion-card wrapper</name>
  <files>src/cloud_usage/dashboard/templates/partials/nios/complete.html, src/cloud_usage/dashboard/templates/partials/ad/complete.html</files>
  <action>
Read both partial files fully before editing. Apply the same restructuring to each.

**For `partials/nios/complete.html`:**

The current outer element is `<article class="completion-banner">`. Change this to `<article class="completion-card">`.

Inside the article, restructure into three sections separated by `<hr>` elements:

```html
<article class="completion-card">
  <header>
    <!-- Existing: Analysis Complete heading + filename display -->
    [keep all existing header content — icon, h2/h3 heading, filename paragraph]
  </header>
  <hr>
  <section>
    <!-- Existing: Token Estimates heading + .scenario-grid with 3 .scenario-card elements -->
    [keep all existing scenario-grid content unchanged]
  </section>
  <hr>
  <section>
    <!-- Existing: Download XLS CTA button/link -->
    [keep all existing download-cta content unchanged]
  </section>
</article>
```

Do not alter any inner content, CSS classes on inner elements, or Jinja2 template variables. Only change:
1. `class="completion-banner"` → `class="completion-card"` on the outer `<article>`
2. Add `<hr>` dividers between the three logical sections
3. Optionally wrap each section in `<header>` / `<section>` semantic elements for clarity

**For `partials/ad/complete.html`:**

Apply the exact same pattern. The existing outer element is also `<article class="completion-banner">`. Change to `<article class="completion-card">` and add `<hr>` dividers between sections (header/analysis-complete area, token breakdown area if present, download/action area if present).

If `partials/ad/complete.html` does not have a download CTA section, use only one `<hr>` between the header and the body content.

IMPORTANT: Do not add accent colors (per locked decision — results/complete screens are accent-neutral). Do not modify `.scenario-card`, `.hero-tokens`, or any inner element classes.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py::test_nios_complete_has_completion_card tests/test_dashboard_visual_redesign.py::test_ad_complete_has_completion_card -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Both tests pass. Both partial files contain `completion-card` and no longer reference `completion-banner` in their outer article element. App starts without template errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add section headers and dividers to Cloud Summary template</name>
  <files>src/cloud_usage/dashboard/templates/pages/summary.html</files>
  <action>
Read `pages/summary.html` in full before editing. The current structure has these key landmarks:
- `{% include "partials/summary_cards.html" %}` at approximately line 10 — no preceding section header
- `<h3>Per-Provider Breakdown</h3>` at approximately line 13
- `<h3>Per-Account Breakdown</h3>` at approximately line 43
- `<h3>Download Reports</h3>` at approximately line 196

Make these targeted insertions:

**1. Before the `{% include "partials/summary_cards.html" %}` line, insert:**
```html
<h3 class="section-header">Token Summary</h3>
```

**2. Before `<h3>Per-Provider Breakdown</h3>`, insert:**
```html
<hr class="section-divider">
```
Then change the existing `<h3>Per-Provider Breakdown</h3>` to `<h3 class="section-header">Per-Provider Breakdown</h3>`.

**3. Before `<h3>Per-Account Breakdown</h3>`, insert:**
```html
<hr class="section-divider">
```
Then change the existing `<h3>Per-Account Breakdown</h3>` to `<h3 class="section-header">Account Attribution</h3>` (rename to "Account Attribution" per CONTEXT.md spec).

**4. Before `<h3>Download Reports</h3>`, insert:**
```html
<hr class="section-divider">
```
Then change the existing `<h3>Download Reports</h3>` to `<h3 class="section-header">Download Reports</h3>`.

Do NOT add card wrappers, do NOT change the data tables or summary_cards include, do NOT alter any Jinja2 template logic. Section headers and dividers only.

Do NOT add accent color to section headers — they use `var(--ib-gray-600)` via the `.section-header` class (neutral, per locked decision).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 11 tests in test_dashboard_visual_redesign.py pass (0 xfailed, 0 errors). summary.html contains "Token Summary" section header. Full suite green.</done>
</task>

</tasks>

<verification>
Run full targeted set: `pytest tests/test_dashboard_visual_redesign.py -x -q`
Expected: 11 passed, 0 xfailed, 0 errors

Full suite: `pytest tests/ -x -q` — must be green with no regressions in test_dashboard_summary.py or test_dashboard_nios.py
</verification>

<success_criteria>
- partials/nios/complete.html outer article uses class="completion-card" with hr dividers between sections
- partials/ad/complete.html same restructuring applied
- pages/summary.html has Token Summary, Per-Provider Breakdown, Account Attribution, Download Reports as h3.section-header with hr.section-divider between each
- app.css defines .completion-card, .section-header, .section-divider
- All 11 xfail tests in test_dashboard_visual_redesign.py pass
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/36-calculator-visual-redesign/36-03-SUMMARY.md`
</output>
