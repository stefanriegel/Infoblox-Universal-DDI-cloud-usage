---
phase: 36-calculator-visual-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - src/cloud_usage/dashboard/routes/pages.py
  - src/cloud_usage/dashboard/templates/base.html
  - src/cloud_usage/dashboard/static/app.css
autonomous: true
requirements:
  - DESIGN-03
  - DESIGN-04

must_haves:
  truths:
    - "GET /cloud returns HTML with body class='calc-cloud'"
    - "GET /nios returns HTML with body class='calc-nios'"
    - "GET /ad returns HTML with body class='calc-ad'"
    - "app.css contains .calc-cloud, .calc-nios, .calc-ad rules with correct hex values"
    - "app.css wizard active/completed selectors use var(--calc-accent) instead of --ib-blue"
    - "app.css completed step has ::after pseudo-element with content '\\2713'"
  artifacts:
    - path: "src/cloud_usage/dashboard/routes/pages.py"
      provides: "calc_theme injected in /cloud, /nios, /ad handlers"
      contains: "calc_theme"
    - path: "src/cloud_usage/dashboard/templates/base.html"
      provides: "body class renders calc_theme via Jinja2 filter"
      contains: "calc_theme | default"
    - path: "src/cloud_usage/dashboard/static/app.css"
      provides: "calc-cloud/nios/ad body classes, --calc-accent wizard selectors, checkmark pseudo-element"
      contains: "var(--calc-accent)"
  key_links:
    - from: "routes/pages.py"
      to: "base.html"
      via: "TemplateResponse context dict calc_theme key"
      pattern: "calc_theme.*calc-cloud"
    - from: "base.html body class"
      to: "app.css .calc-cloud/.calc-nios/.calc-ad"
      via: "CSS cascade on body element"
      pattern: "class.*calc_theme"
    - from: "app.css .calc-* body classes"
      to: ".wizard-steps selectors"
      via: "--calc-accent custom property cascade"
      pattern: "var\\(--calc-accent\\)"
---

<objective>
Implement the per-calculator accent system (DESIGN-03) and wizard step visual progression (DESIGN-04).

Injects `calc_theme` into three route handlers, renders it as a body CSS class in `base.html`, defines three `.calc-*` body class rules in `app.css` that set `--calc-accent`, and swaps all wizard step `--ib-blue` references to `var(--calc-accent)`. Also adds the completed step checkmark via CSS `::after` pseudo-element.

Purpose: Makes Cloud, NIOS, and AD calculators visually distinguishable by accent color on wizard screens, with completed steps showing a checkmark instead of their step number.
Output: Three modified files; 8 of 11 xfail tests in test_dashboard_visual_redesign.py turn green.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-calculator-visual-redesign/36-RESEARCH.md

<interfaces>
<!-- Route handler current structure (routes/pages.py) — Phase 35 established calculator_name pattern -->
@router.get("/cloud", response_class=HTMLResponse)
async def cloud_calculator(request: Request) -> HTMLResponse:
    templates = request.app.state.templates
    context = _get_tab_context(request, "progress")
    context["calculator_name"] = "Cloud Calculator"   # Phase 35 — keep as-is
    # Phase 36 adds:
    context["calc_theme"] = "calc-cloud"
    return templates.TemplateResponse(request, "base.html", context)

<!-- Same pattern for /nios → "calc-nios", /ad → "calc-ad" -->

<!-- base.html current body tag (line 10): -->
<body>
<!-- Phase 36 change: -->
<body class="{{ calc_theme | default('') }}">

<!-- app.css wizard section (lines 360–390) — current --ib-blue references to replace: -->
.wizard-steps li.active          { color: var(--ib-blue); }         → var(--calc-accent)
.wizard-steps li.active .step-number { background/border-color: var(--ib-blue); } → var(--calc-accent)
.wizard-steps li.completed       { color: var(--ib-blue); }         → var(--calc-accent)
.wizard-steps li.completed .step-number { background/border-color: var(--ib-blue); } → var(--calc-accent)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject calc_theme in route handlers and body class in base.html</name>
  <files>src/cloud_usage/dashboard/routes/pages.py, src/cloud_usage/dashboard/templates/base.html</files>
  <action>
**routes/pages.py:** In each of the three page-level route handlers, add `context["calc_theme"] = "calc-{calculator}"` immediately after the `context["calculator_name"] = "..."` line. Do NOT modify `_get_tab_context()` — this helper stays generic (locked decision).

- `/cloud` handler: `context["calc_theme"] = "calc-cloud"`
- `/nios` handler: `context["calc_theme"] = "calc-nios"`
- `/ad` handler: `context["calc_theme"] = "calc-ad"`

The home route (`/`) is served by `home.html` which does not extend `base.html` — no `calc_theme` needed there.

**base.html:** Find the `<body>` tag (line 10, currently `<body>`) and change it to:
```html
<body class="{{ calc_theme | default('') }}">
```

The `| default('')` produces `class=""` on any future route that uses `base.html` without providing `calc_theme`. This is valid HTML and safe to leave.

Do not modify any other part of `base.html`. The breadcrumb block added in Phase 35 must remain intact.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py::test_cloud_body_has_calc_cloud_class tests/test_dashboard_visual_redesign.py::test_nios_body_has_calc_nios_class tests/test_dashboard_visual_redesign.py::test_ad_body_has_calc_ad_class -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>GET /cloud, /nios, /ad each return 200 with the correct `class="calc-*"` on the `<body>` tag. Three xfail stubs turn green (XPASS would be a failure — they must PASS, meaning xfail markers should be removed in Plan 01 task if strict=True causes XPASS=failure; alternatively these 3 tests should now pass cleanly).</done>
</task>

<task type="auto">
  <name>Task 2: Add accent CSS rules and update wizard selectors in app.css</name>
  <files>src/cloud_usage/dashboard/static/app.css</files>
  <action>
Make three additions/changes to `app.css`:

**A. Add `--calc-accent` fallback to `:root` in design-system.css (defensive):**
Actually add to `app.css` at the top of the file (or alongside other `:root` definitions if any exist in app.css). Add:
```css
:root {
    --calc-accent: var(--ib-blue);  /* fallback when no calc-* body class is present */
}
```
If `app.css` already has a `:root` block, append to it. If not, add a new block near the top.

**B. Add three body-class accent rules (after the `:root` block or in a clearly labeled "Per-Calculator Themes" section):**
```css
/* Per-Calculator Themes */
.calc-cloud { --calc-accent: #0066CC; }
.calc-nios  { --calc-accent: #00C389; }
.calc-ad    { --calc-accent: #8B5CF6; }
```

**C. Update wizard-steps active/completed selectors — replace `var(--ib-blue)` with `var(--calc-accent)`:**

Find the `.wizard-steps li.active`, `.wizard-steps li.active .step-number`, `.wizard-steps li.completed`, `.wizard-steps li.completed .step-number` blocks (around lines 360–390). Replace every occurrence of `var(--ib-blue)` within these four rule blocks with `var(--calc-accent)`.

Do NOT replace `var(--ib-blue)` anywhere else in the file. Only wizard-steps active/completed selectors get the accent. Tab bar, hero-tokens, download-cta buttons all retain `--ib-blue`.

**D. Add completed step checkmark pseudo-element:**

After `.wizard-steps li.completed .step-number { ... }`, add:
```css
.wizard-steps li.completed .step-number {
    position: relative;  /* anchor for ::after absolute positioning */
    font-size: 0;        /* hide the step number text */
    color: transparent;
}
.wizard-steps li.completed .step-number::after {
    content: '\2713';
    color: #fff;
    font-size: 0.85rem;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}
```

Note: The existing `.wizard-steps li.completed .step-number` block already sets `background`, `color: #fff`, `border-color`. The Phase 36 change adds `position: relative`, overrides `color` to `transparent`, sets `font-size: 0`, and adds the `::after` rule. Merge these into the existing block rather than duplicating — edit the existing rule to add `position: relative; font-size: 0; color: transparent;` and append the `::after` rule immediately after.

The `.step-number` span is `display: inline-flex` (confirmed in app.css line 370) which fully supports `::after` pseudo-elements.

Do NOT change any wizard-steps CSS beyond the four active/completed selector blocks described above.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py::test_app_css_has_calc_cloud_accent tests/test_dashboard_visual_redesign.py::test_app_css_has_calc_nios_accent tests/test_dashboard_visual_redesign.py::test_app_css_has_calc_ad_accent tests/test_dashboard_visual_redesign.py::test_app_css_wizard_completed_uses_calc_accent tests/test_dashboard_visual_redesign.py::test_app_css_wizard_completed_has_checkmark -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>All 5 targeted tests pass. app.css contains .calc-cloud/#0066CC, .calc-nios/#00C389, .calc-ad/#8B5CF6, var(--calc-accent) in wizard selectors, and \2713 in a ::after pseudo-element. Full suite still green.</done>
</task>

</tasks>

<verification>
Run full targeted set: `pytest tests/test_dashboard_visual_redesign.py -x -q`
Expected: 8 passed (DESIGN-03: 6, DESIGN-04: 2), 3 xfailed (DESIGN-05 — addressed in Plan 03)

Full suite: `pytest tests/ -x -q` — must be green
</verification>

<success_criteria>
- GET /cloud, /nios, /ad responses each contain correct body class (calc-cloud, calc-nios, calc-ad)
- app.css has three .calc-* body class rules with exact hex values from requirements
- app.css wizard active/completed selectors use var(--calc-accent), not --ib-blue
- app.css has ::after checkmark pseudo-element with content '\2713' on completed steps
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/36-calculator-visual-redesign/36-02-SUMMARY.md`
</output>
