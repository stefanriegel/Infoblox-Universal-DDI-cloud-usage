---
phase: 36-calculator-visual-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard_visual_redesign.py
autonomous: true
requirements:
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05

must_haves:
  truths:
    - "tests/test_dashboard_visual_redesign.py exists with 11 xfail stubs"
    - "Running pytest tests/test_dashboard_visual_redesign.py shows 11 xfailed, 0 errors"
    - "Full test suite passes with the new file present"
  artifacts:
    - path: "tests/test_dashboard_visual_redesign.py"
      provides: "xfail stubs covering DESIGN-03 (6), DESIGN-04 (2), DESIGN-05 (3)"
      exports: []
  key_links:
    - from: "tests/test_dashboard_visual_redesign.py"
      to: "cloud_usage.dashboard.app.create_app"
      via: "TestClient import pattern"
      pattern: "from cloud_usage.dashboard.app import create_app"
---

<objective>
Create the xfail test scaffold for Phase 36. All 11 stubs must fail before implementation (Waves 2 and 3), then turn green when Plans 02 and 03 deliver the accent system and card layout changes.

Purpose: Nyquist compliance — tests define acceptance criteria before implementation so regressions are caught immediately.
Output: tests/test_dashboard_visual_redesign.py with 11 xfail(strict=True) stubs covering DESIGN-03, DESIGN-04, and DESIGN-05.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-calculator-visual-redesign/36-RESEARCH.md

<interfaces>
<!-- Confirmed import pattern from tests/test_dashboard_design.py and test_dashboard_navigation.py -->
from __future__ import annotations
import sys
from pathlib import Path
import pytest
sys.path.insert(0, str(Path(__file__).parent.parent / "src"))
from starlette.testclient import TestClient
from cloud_usage.dashboard.app import create_app

app = create_app()
# Each test uses: with TestClient(app) as client: response = client.get(...)
# CSS assertions: client.get("/static/app.css") returns full CSS text
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_dashboard_visual_redesign.py with 11 xfail stubs</name>
  <files>tests/test_dashboard_visual_redesign.py</files>
  <action>
Create `tests/test_dashboard_visual_redesign.py`. All 11 stubs marked `@pytest.mark.xfail(strict=True, reason="Phase 36 Plan 02/03 not yet implemented")`.

Use the starlette TestClient (not fastapi TestClient) — matches the pattern in test_dashboard_design.py.

Module docstring: "Wave 0 xfail scaffold for Phase 36 Calculator Visual Redesign (DESIGN-03, DESIGN-04, DESIGN-05)."

**DESIGN-03 stubs (6 tests) — accent color body class and CSS rules:**

1. `test_cloud_body_has_calc_cloud_class`
   — GET /cloud, assert `'class="calc-cloud"'` in response.text

2. `test_nios_body_has_calc_nios_class`
   — GET /nios, assert `'class="calc-nios"'` in response.text

3. `test_ad_body_has_calc_ad_class`
   — GET /ad, assert `'class="calc-ad"'` in response.text

4. `test_app_css_has_calc_cloud_accent`
   — GET /static/app.css (200), assert `.calc-cloud` in response.text AND `#0066CC` in response.text

5. `test_app_css_has_calc_nios_accent`
   — GET /static/app.css (200), assert `.calc-nios` in response.text AND `#00C389` in response.text

6. `test_app_css_has_calc_ad_accent`
   — GET /static/app.css (200), assert `.calc-ad` in response.text AND `#8B5CF6` in response.text

**DESIGN-04 stubs (2 tests) — wizard step checkmark and calc-accent usage:**

7. `test_app_css_wizard_completed_uses_calc_accent`
   — GET /static/app.css (200), assert `var(--calc-accent)` in response.text

8. `test_app_css_wizard_completed_has_checkmark`
   — GET /static/app.css (200), assert `\\2713` in response.text

**DESIGN-05 stubs (3 tests) — card layout on results/complete screens:**

9. `test_nios_complete_has_completion_card`
   — Read `src/cloud_usage/dashboard/templates/partials/nios/complete.html` as text, assert `completion-card` in content
   — Use `Path(__file__).parent.parent / "src/cloud_usage/dashboard/templates/partials/nios/complete.html"` to get absolute path; read via `Path.read_text()`; no HTTP client needed

10. `test_ad_complete_has_completion_card`
    — Same pattern for `partials/ad/complete.html`

11. `test_summary_has_token_summary_header`
    — Read `src/cloud_usage/dashboard/templates/pages/summary.html` as text, assert `Token Summary` in content

Template file tests (9, 10, 11) read source files directly — they do not require manager state manipulation. This avoids complexity of mocking a complete scan state.

Each test body follows this pattern:
```python
@pytest.mark.xfail(strict=True, reason="Phase 36 Plan 02/03 not yet implemented")
def test_cloud_body_has_calc_cloud_class() -> None:
    with TestClient(app) as client:
        response = client.get("/cloud")
    assert response.status_code == 200
    assert 'class="calc-cloud"' in response.text
```

For template file tests (9–11), the pattern is:
```python
@pytest.mark.xfail(strict=True, reason="Phase 36 Plan 03 not yet implemented")
def test_nios_complete_has_completion_card() -> None:
    tmpl = Path(__file__).parent.parent / "src/cloud_usage/dashboard/templates/partials/nios/complete.html"
    content = tmpl.read_text()
    assert "completion-card" in content
```

Do NOT use strict=False. Do NOT mix non-xfail tests. All 11 must be xfail(strict=True).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports 11 xfailed, 0 passed, 0 errors. All stubs present with correct assertion bodies and strict=True.</done>
</task>

</tasks>

<verification>
Run: `source venv/bin/activate && python -m pytest tests/test_dashboard_visual_redesign.py -v`
Expected: 11 xfailed (XFAIL), 0 errors

Full suite still green: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- tests/test_dashboard_visual_redesign.py exists with 11 xfail(strict=True) stubs
- pytest tests/test_dashboard_visual_redesign.py shows 11 xfailed, 0 errors
- Full suite passes — no regressions from adding the test file
- Stubs cover DESIGN-03 (6), DESIGN-04 (2), DESIGN-05 (3) as documented in 36-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/36-calculator-visual-redesign/36-01-SUMMARY.md`
</output>
