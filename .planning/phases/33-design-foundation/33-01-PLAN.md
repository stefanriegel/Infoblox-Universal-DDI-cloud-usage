---
phase: 33-design-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_dashboard_design.py
  - tests/test_dashboard_app.py
autonomous: true
requirements:
  - DESIGN-01

must_haves:
  truths:
    - "tests/test_dashboard_design.py exists with xfail stubs covering all DESIGN-01 assertions"
    - "test_static_pico_css_served is renamed to test_static_design_system_css_served in test_dashboard_app.py"
    - "All new design tests fail (xfail) when run against unmodified codebase"
  artifacts:
    - path: "tests/test_dashboard_design.py"
      provides: "DESIGN-01 test stubs: CSS file serving, token presence, no-pico assertions, font serving, no data-theme"
      exports: ["test_base_html_no_pico_reference", "test_base_html_references_design_system", "test_base_html_no_data_theme", "test_design_system_contains_ib_navy", "test_design_system_contains_ib_accent_green", "test_app_css_no_root_variables", "test_no_pico_variable_references", "test_inter_fonts_served"]
    - path: "tests/test_dashboard_app.py"
      provides: "Updated static file serving test for design-system.css"
      contains: "test_static_design_system_css_served"
  key_links:
    - from: "tests/test_dashboard_design.py"
      to: "src/cloud_usage/dashboard/static/design-system.css"
      via: "HTTP GET /static/design-system.css via TestClient"
      pattern: "client\\.get.*design-system\\.css"
    - from: "tests/test_dashboard_design.py"
      to: "src/cloud_usage/dashboard/templates/base.html"
      via: "HTTP GET / response body parse"
      pattern: "client\\.get.*/"
---

<objective>
Create Wave 0 test scaffold for Phase 33 — all test stubs that define DESIGN-01 acceptance criteria before any implementation begins.

Purpose: Nyquist compliance — every task in waves 1 and 2 must have a failing test to turn green. Writing tests first makes the acceptance criteria concrete and verifiable.
Output: tests/test_dashboard_design.py (new, 9 xfail stubs) + test_dashboard_app.py (renamed pico test).
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-design-foundation/33-CONTEXT.md
@.planning/phases/33-design-foundation/33-VALIDATION.md

<interfaces>
<!-- From tests/test_dashboard_app.py — existing pattern to follow -->
<!-- TestAppFactory class, pytest fixtures, TestClient usage -->

From tests/test_dashboard_app.py (pattern to follow):
```python
from starlette.testclient import TestClient
from cloud_usage.dashboard.app import create_app

class TestAppFactory:
    def test_static_pico_css_served(self) -> None:
        """GET /static/pico.min.css returns 200."""
        with TestClient(create_app()) as client:
            response = client.get("/static/pico.min.css")
            assert response.status_code == 200

    def test_static_app_css_served(self) -> None:
        """GET /static/app.css returns 200."""
        with TestClient(create_app()) as client:
            response = client.get("/static/app.css")
            assert response.status_code == 200
```

Test run command: source venv/bin/activate && python -m pytest tests/test_dashboard_design.py -x -q
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_dashboard_design.py with xfail stubs</name>
  <files>tests/test_dashboard_design.py</files>
  <behavior>
    - test_base_html_no_pico_reference: GET / response body must NOT contain "pico.min.css" — xfail because base.html still has the pico link
    - test_base_html_references_design_system: GET / response body must contain "design-system.css" — xfail because the link doesn't exist yet
    - test_base_html_no_data_theme: GET / response body must NOT contain 'data-theme' — xfail because base.html still has data-theme="light"
    - test_design_system_contains_ib_navy: GET /static/design-system.css returns 200 and body contains "--ib-navy" — xfail because file doesn't exist
    - test_design_system_contains_ib_accent_green: GET /static/design-system.css returns 200 and body contains "--ib-accent-green" — xfail
    - test_design_system_contains_page_bg: GET /static/design-system.css returns 200 and body contains "--page-bg" — xfail
    - test_app_css_no_root_variables: GET /static/app.css returns 200 and body must NOT contain "--ib-blue:" in a :root block — xfail because app.css still has :root variables
    - test_no_pico_variable_references: GET / response body must NOT contain "--pico-" — xfail because templates still reference --pico-color-red-500 and --pico-muted-color
    - test_inter_fonts_served: GET /static/inter-variable.woff2 returns 200 — xfail because font file doesn't exist yet
  </behavior>
  <action>
Create `tests/test_dashboard_design.py`. Use `pytest.mark.xfail(strict=False, reason="Wave 0 scaffold — will pass after implementation")` on each test. Use `TestClient(create_app())` as context manager, same pattern as test_dashboard_app.py.

Structure:
1. Imports: `import pytest`, `from starlette.testclient import TestClient`, `from cloud_usage.dashboard.app import create_app`
2. Module-level `app = create_app()` for reuse across tests
3. Each test uses `with TestClient(app) as client:` to make the request
4. For HTML tests: assert on `response.text`
5. For CSS/font tests: assert on `response.status_code == 200` and `response.text` for CSS content checks

test_app_css_no_root_variables check: GET /static/app.css, parse response.text, assert ":root" not in the CSS body (once variables are migrated, the :root block will be removed). Mark xfail.

test_no_pico_variable_references: GET / to get the rendered index page, assert "--pico-" NOT in response.text. Mark xfail since templates still have --pico references.

Note: GET / will render the base template which contains the tab-container HTMX div — for assertions about base.html attributes (data-theme, CSS links) this is sufficient since they appear in the outer HTML shell.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_design.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 9 tests in test_dashboard_design.py are collected and marked xfail; pytest exits 0 (xfail counts as pass). No unexpected errors or import failures.</done>
</task>

<task type="auto">
  <name>Task 2: Rename pico test in test_dashboard_app.py</name>
  <files>tests/test_dashboard_app.py</files>
  <action>
In `tests/test_dashboard_app.py`, find the test method `test_static_pico_css_served` (around line 71). Make two changes:
1. Rename the method to `test_static_design_system_css_served`
2. Update the docstring to: "GET /static/design-system.css returns 200."
3. Update the URL in client.get() from "/static/pico.min.css" to "/static/design-system.css"
4. Mark the test with `@pytest.mark.xfail(strict=False, reason="Wave 0 scaffold — design-system.css not yet created")` so it passes before Wave 1 ships the file.

Add `import pytest` at the top if not already present.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_app.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_static_pico_css_served no longer exists. test_static_design_system_css_served is present, marked xfail, and the full test_dashboard_app.py suite passes.</done>
</task>

</tasks>

<verification>
Full Wave 0 check:
```
source venv/bin/activate && python -m pytest tests/test_dashboard_design.py tests/test_dashboard_app.py -v -q 2>&1 | tail -20
```
Expected: all tests pass (xfail counts as pass), 0 errors.
</verification>

<success_criteria>
- tests/test_dashboard_design.py exists with 9 xfail test stubs covering all DESIGN-01 acceptance criteria
- tests/test_dashboard_app.py has test_static_design_system_css_served (renamed from test_static_pico_css_served), marked xfail
- `python -m pytest tests/test_dashboard_design.py tests/test_dashboard_app.py -q` exits 0
- No pico.min.css reference remains in test assertions
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-foundation/33-01-SUMMARY.md`
</output>
