---
phase: 33-design-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 33-01
  - 33-02
files_modified:
  - src/cloud_usage/dashboard/templates/base.html
  - src/cloud_usage/dashboard/static/app.css
  - src/cloud_usage/dashboard/templates/pages/nios.html
  - src/cloud_usage/dashboard/templates/pages/ad.html
  - src/cloud_usage/dashboard/templates/partials/nios/step1_upload.html
  - src/cloud_usage/dashboard/templates/partials/nios/complete.html
  - src/cloud_usage/dashboard/templates/partials/ad/complete.html
  - src/cloud_usage/dashboard/templates/pages/summary.html
autonomous: false
requirements:
  - DESIGN-01

must_haves:
  truths:
    - "PicoCSS is removed from all templates — no pico.min.css link and no --pico-* variable references remain anywhere"
    - "base.html loads design-system.css instead of pico.min.css, and data-theme attribute is removed"
    - "app.css no longer defines :root CSS variables — all --ib-* variables are exclusively in design-system.css"
    - "All existing calculator flows (Cloud, NIOS, AD) remain functional with the new CSS"
    - "role='button' on <a> elements is replaced with .btn class; aria-busy pattern is updated"
  artifacts:
    - path: "src/cloud_usage/dashboard/templates/base.html"
      provides: "Updated base template without pico.min.css link and without data-theme attribute"
      contains: "design-system.css"
    - path: "src/cloud_usage/dashboard/static/app.css"
      provides: "Component styles only, no :root variable definitions"
    - path: "src/cloud_usage/dashboard/static/pico.min.css"
      provides: "DELETED — no longer present"
  key_links:
    - from: "src/cloud_usage/dashboard/templates/base.html"
      to: "src/cloud_usage/dashboard/static/design-system.css"
      via: "<link rel='stylesheet' href='{{ url_for(static, design-system.css) }}'>"
      pattern: "design-system\\.css"
    - from: "templates (all)"
      to: "design-system.css :root"
      via: "var(--ib-*) references (no --pico-* references remain)"
      pattern: "var\\(--ib-"
---

<objective>
Wire in the new design system: swap the CSS link in base.html, purge the :root block from app.css, remove all --pico-* variable references from templates, convert role="button" links to .btn class, and delete pico.min.css.

Purpose: This plan makes the design system live. After this plan, PicoCSS is gone entirely and design-system.css is the only base stylesheet.
Output: Updated base.html, cleaned app.css, 5 template files with --pico references fixed, pico.min.css deleted.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/33-design-foundation/33-CONTEXT.md
@.planning/phases/33-design-foundation/33-RESEARCH.md
@.planning/phases/33-design-foundation/33-02-SUMMARY.md

<interfaces>
<!-- Complete inventory of all changes required in this plan -->

base.html changes (3 edits):
  1. Line 2: <html lang="en" data-theme="light"> → <html lang="en">
  2. Line 7: <link rel="stylesheet" href="{{ url_for('static', path='pico.min.css') }}">
             → <link rel="stylesheet" href="{{ url_for('static', path='design-system.css') }}">
  3. (no third change needed — the above two cover it)

app.css changes (1 edit):
  Remove lines 1-17 (the :root { --ib-* } block). Keep everything from line 18 onward.
  The "/* UDDI Estimator -- Custom CSS overrides */" comment block at top should be updated
  to note that variables are now in design-system.css.

--pico-* template replacements:
  partials/nios/step1_upload.html:
    - line 27: var(--pico-color-red-500,#e74c3c) → var(--ib-red)
    - line 32: var(--pico-color-red-500, #e74c3c) → var(--ib-red)  (article style attr border-left)
    - line 33: var(--pico-color-red-500, #e74c3c) → var(--ib-red)
    - line 70: var(--pico-muted-color, #666) → var(--ib-gray-600)
  pages/nios.html:
    - line 37: var(--pico-color-red-500, #e74c3c) → var(--ib-red)
  pages/ad.html:
    - line 39: var(--pico-color-red-500, #e74c3c) → var(--ib-red)

role="button" on <a> conversions (per CONTEXT.md decision):
  partials/nios/complete.html:80 — add class="btn" to the <a> element (keep href, keep download attribute if present)
  partials/ad/complete.html:84 — add class="btn" to the <a> element
  pages/summary.html:201 — change class="outline" role="button" to class="btn outline" (remove role="button", keep class)

aria-busy on <p>:
  partials/nios/step1_upload.html:25 — <p aria-busy="true"> is handled by design-system.css
  (design-system.css already styles [aria-busy="true"] — no template change needed, just verify it renders acceptably)

pico.min.css deletion:
  Delete src/cloud_usage/dashboard/static/pico.min.css
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap CSS in base.html, purge :root from app.css, delete pico.min.css</name>
  <files>
    src/cloud_usage/dashboard/templates/base.html,
    src/cloud_usage/dashboard/static/app.css,
    src/cloud_usage/dashboard/static/pico.min.css
  </files>
  <action>
**base.html (2 edits):**
1. Remove `data-theme="light"` from the `<html>` tag: `<html lang="en" data-theme="light">` → `<html lang="en">`
2. Replace the pico link with design-system link:
   `{{ url_for('static', path='pico.min.css') }}` → `{{ url_for('static', path='design-system.css') }}`
   Keep app.css link as-is. Order must be: design-system.css first, then app.css.

**app.css (1 edit):**
Remove the entire `:root { ... }` block (lines 1-17 including the `/* ===== Color palette ===== */` comment). Replace the top comment to read:
```css
/* UDDI Estimator -- Component styles.
   Design tokens (--ib-* variables) are defined in design-system.css :root.
   This file contains dashboard-specific component styles only. */
```
Leave all other rules in app.css completely unchanged. The body background, .site-header, .tab-bar, .wizard-steps, .summary-cards, etc. all stay in app.css — they reference var(--ib-*) variables which will now resolve from design-system.css.

**pico.min.css:**
Delete the file: `rm src/cloud_usage/dashboard/static/pico.min.css`

This is the point-of-no-return: once pico.min.css is deleted, the only CSS loaded is design-system.css + app.css. If design-system.css is incomplete, visual regressions will appear. Verify design-system.css is confirmed complete from Plan 02 before this step.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_design.py::test_base_html_no_pico_reference tests/test_dashboard_design.py::test_base_html_references_design_system tests/test_dashboard_design.py::test_base_html_no_data_theme tests/test_dashboard_design.py::test_app_css_no_root_variables tests/test_dashboard_app.py::TestAppFactory::test_static_design_system_css_served -v 2>&1 | tail -15</automated>
  </verify>
  <done>base.html contains design-system.css link and no pico.min.css link. data-theme attribute is gone. app.css has no :root block. pico.min.css is deleted from static/. All five targeted tests pass (no longer xfail).</done>
</task>

<task type="auto">
  <name>Task 2: Replace --pico-* references in templates and fix role="button" links</name>
  <files>
    src/cloud_usage/dashboard/templates/pages/nios.html,
    src/cloud_usage/dashboard/templates/pages/ad.html,
    src/cloud_usage/dashboard/templates/partials/nios/step1_upload.html,
    src/cloud_usage/dashboard/templates/partials/nios/complete.html,
    src/cloud_usage/dashboard/templates/partials/ad/complete.html,
    src/cloud_usage/dashboard/templates/pages/summary.html
  </files>
  <action>
**Replace all --pico-* variable references:**

`partials/nios/step1_upload.html` — 4 occurrences:
- Line 27: `var(--pico-color-red-500,#e74c3c)` → `var(--ib-red)`
- Line 32: `var(--pico-color-red-500, #e74c3c)` → `var(--ib-red)` (inside article style attribute for border-left color)
- Line 33: `var(--pico-color-red-500, #e74c3c)` → `var(--ib-red)`
- Line 70: `var(--pico-muted-color, #666)` → `var(--ib-gray-600)`

`pages/nios.html` — 1 occurrence:
- Line 37: `var(--pico-color-red-500, #e74c3c)` → `var(--ib-red)`

`pages/ad.html` — 1 occurrence:
- Line 39: `var(--pico-color-red-500, #e74c3c)` → `var(--ib-red)`

**Fix role="button" on <a> links (per CONTEXT.md locked decision):**

`partials/nios/complete.html` around line 80:
Find `<a` with `role="button"`. Add `class="btn"` to this element. Remove `role="button"` attribute. Keep all other attributes (href, download, hx-* if present) intact.

`partials/ad/complete.html` around line 84:
Same as above — add `class="btn"`, remove `role="button"`.

`pages/summary.html` around line 201:
Find `<a href="{{ d.url }}" class="outline" role="button" ...>`. Change to `<a href="{{ d.url }}" class="btn outline" ...>` — remove `role="button"`, change class from `"outline"` to `"btn outline"`.

**Post-change verification grep:**
After making all changes, confirm no `--pico` strings remain in any template file. The test_no_pico_variable_references test will catch any missed occurrences.

DO NOT touch HTMX attributes (hx-get, hx-post, hx-target, hx-swap, hx-trigger, sse-*, hx-on:*). Do NOT modify #tab-container, #main-content, or any element ID used as an HTMX target. Only change inline style attribute values and class/role attributes on the specific elements listed above.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_design.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests in test_dashboard_design.py pass (no xfail remaining). No --pico-* references in any template. No role="button" on <a> elements. Full pytest suite passes without regressions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete PicoCSS replacement: design-system.css is live, Inter font self-hosted, all templates cleaned. The app now runs exclusively on the custom Infoblox brand design system.
  </what-built>
  <how-to-verify>
    1. Start the app: `source venv/bin/activate && uvicorn cloud_usage.dashboard.app:app --reload`
    2. Open http://localhost:8000 in a browser
    3. Verify visual basics:
       - Page background is light gray (#F8F9FA) — not white
       - Header/nav area visible and styled (not bare HTML)
       - Text renders in Inter font (check DevTools > Elements > Computed > font-family)
       - Primary buttons are solid blue (#0066CC), outline buttons are transparent with blue border
    4. Click through the Cloud Calculator wizard (step 1-4) — verify no broken layouts, forms render correctly, progress bar is visible
    5. Open the NIOS tab — verify no broken layout, error messages show in red
    6. Open the AD tab — verify no broken layout
    7. Confirm in DevTools Network tab: pico.min.css is NOT loaded, design-system.css IS loaded (200)
    8. Run full test suite: `source venv/bin/activate && python -m pytest tests/ -x -q`
  </how-to-verify>
  <resume-signal>Type "approved" if the visual check passes, or describe any specific layout issues seen</resume-signal>
</task>

</tasks>

<verification>
Full phase gate before /gsd:verify-work:
```
source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -20
```
Expected: all tests pass, no pico references in any grep output.

Additional grep check:
```
grep -r "--pico" src/cloud_usage/dashboard/templates/ && echo "FAIL: pico refs found" || echo "OK: no pico refs"
grep -r "pico.min.css" src/cloud_usage/dashboard/templates/ && echo "FAIL: pico link found" || echo "OK: no pico link"
```
</verification>

<success_criteria>
- base.html: loads design-system.css (not pico.min.css), no data-theme attribute
- app.css: no :root block, all component styles intact
- pico.min.css: deleted from static/
- 0 occurrences of --pico-* in any template file
- 0 occurrences of role="button" on <a> elements (converted to .btn class)
- All test_dashboard_design.py tests pass (no longer xfail)
- Full pytest suite green: python -m pytest tests/ -x -q
- Visual smoke: Cloud, NIOS, AD tabs render correctly with brand styling in browser
</success_criteria>

<output>
After completion, create `.planning/phases/33-design-foundation/33-03-SUMMARY.md`
</output>
