---
phase: 19-token-breakdown-webui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cloud_usage/dashboard/templates/partials/nios/complete.html
autonomous: true
requirements:
  - BRKDN-01
  - BRKDN-02

must_haves:
  truths:
    - "After NIOS analysis completes, each scenario card shows raw DDI, Active IP, and Asset counts above the hero token number"
    - "Each scenario card shows formula derivation rows in the format '1,234 DDI ÷ 50 = 24.7' per component, then 'Total: X.X tokens'"
    - "The Hybrid UDDI card renders two sub-blocks (NIOS-remaining and NIOSX-migrated) each with their own counts and formula, plus a combined total"
    - "Assets row is present in the template but only rendered when asset_count > 0"
    - "GET /tab/nios in COMPLETE state returns 200 and the breakdown markup is present"
  artifacts:
    - src/cloud_usage/dashboard/templates/partials/nios/complete.html
  key_links:
    - "scenario_suite.current_grid, scenario_suite.full_migration, scenario_suite.hybrid_uddi → complete.html scenario cards"
---

<objective>
Expand the three NIOS scenario cards in `complete.html` to show raw object counts and inline formula derivation below each hero token number.

Purpose: Customers need to see the math behind each token total without opening the XLS. The current UI shows only the final token number; this plan adds the DDI/IP/Asset counts and the formula steps inline.

Output: Modified `complete.html` where each scenario card contains formula-derivation rows. No backend changes — all data is already present in `scenario_suite`.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-token-breakdown-webui/19-CONTEXT.md

<interfaces>
<!-- Key data available in complete.html template context -->

ScenarioSuite (scenario_suite):
  current_grid: ScenarioResult
  full_migration: ScenarioResult
  hybrid_uddi: HybridScenarioResult | None
  member_attribution: List[MemberScenarioRow]

ScenarioResult fields:
  ddi_count: int
  active_ip_count: int
  asset_count: int
  token_total: float
  formula_name: str   # e.g. "NIOS Object (DDI/50 + IPs/25 + Assets/13)"

HybridScenarioResult fields:
  nios_sub: ScenarioResult   # name="nios_remaining"
  niosx_sub: ScenarioResult  # name="niosx_migrated"
  combined_total: float

Existing CSS classes (app.css):
  .scenario-grid        — CSS grid, auto-fit minmax(200px,1fr), gap 1rem
  .scenario-card        — blue-light bg, border, border-radius 8px, text-align center
  .scenario-card.primary — blue bg, white text
  .sc-label             — 0.75rem, uppercase, 0.75 opacity, display block, mb 0.4rem
  .hero-tokens          — 3rem, font-weight 800, color ib-blue, display block

Jinja2 filter available: {{ value | int }} for integer truncation.
For comma-formatted numbers use: {{ "{:,}".format(value | int) }}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formula breakdown rows to scenario cards in complete.html</name>
  <files>src/cloud_usage/dashboard/templates/partials/nios/complete.html</files>
  <behavior>
    - The rendered HTML for current_grid card contains the string "DDI ÷ 50"
    - The rendered HTML for full_migration card contains the string "DDI ÷ 25"
    - The rendered HTML contains comma-formatted DDI count (e.g. "1,234" not "1234" for values ≥ 1000)
    - When scenario_suite.hybrid_uddi is not None, the hybrid card renders both "NIOS-remaining" and "NIOSX-migrated" sub-blocks
    - The Assets row is not rendered when asset_count == 0
    - The hero token number still appears (existing .hero-tokens span preserved)
    - GET /tab/nios with nios_manager in COMPLETE state returns 200 and response text contains "DDI ÷"
  </behavior>
  <action>
Modify `src/cloud_usage/dashboard/templates/partials/nios/complete.html`.

**Layout for each standard scenario card (current_grid and full_migration):**

Below the `.hero-tokens` span and above `<small>tokens</small>`, insert a `<div class="sc-breakdown">` block:

```html
<div class="sc-breakdown" style="margin-top:0.75rem; text-align:left; font-size:0.8rem; line-height:1.6;">
  <div>{{ "{:,}".format(sr.ddi_count) }} DDI ÷ {{ divisor_ddi }} = {{ "%.1f"|format(sr.ddi_count / divisor_ddi) }}</div>
  <div>{{ "{:,}".format(sr.active_ip_count) }} IPs ÷ {{ divisor_ip }} = {{ "%.1f"|format(sr.active_ip_count / divisor_ip) }}</div>
  {% if sr.asset_count > 0 %}
  <div>{{ "{:,}".format(sr.asset_count) }} Assets ÷ {{ divisor_asset }} = {{ "%.1f"|format(sr.asset_count / divisor_asset) }}</div>
  {% endif %}
  <div style="border-top:1px solid currentColor; opacity:0.3; margin:0.25rem 0;"></div>
  <div><strong>Total: {{ "%.1f"|format(sr.token_total) }} tokens</strong></div>
</div>
```

Because Jinja2 doesn't allow arithmetic in templates without custom filters, hardcode the divisor values directly (they come from counter.py constants that never change):
- NIOS (current_grid): DDI ÷ 50, IPs ÷ 25, Assets ÷ 13
- UDDI native (full_migration): DDI ÷ 25, IPs ÷ 13, Assets ÷ 3

Use Jinja2's `"%.1f"|format(value)` filter for one-decimal formatting.

**Updated current_grid card:**
```html
<div class="scenario-card">
  <span class="sc-label">Current Grid (NIOS Objects)</span>
  <span class="hero-tokens">{{ scenario_suite.current_grid.token_total | int }}</span>
  <small>tokens</small>
  <div class="sc-breakdown" style="margin-top:0.75rem; text-align:left; font-size:0.8rem; line-height:1.6;">
    <div>{{ "{:,}".format(scenario_suite.current_grid.ddi_count) }} DDI ÷ 50 = {{ "%.1f"|format(scenario_suite.current_grid.ddi_count / 50) }}</div>
    <div>{{ "{:,}".format(scenario_suite.current_grid.active_ip_count) }} IPs ÷ 25 = {{ "%.1f"|format(scenario_suite.current_grid.active_ip_count / 25) }}</div>
    {% if scenario_suite.current_grid.asset_count > 0 %}
    <div>{{ "{:,}".format(scenario_suite.current_grid.asset_count) }} Assets ÷ 13 = {{ "%.1f"|format(scenario_suite.current_grid.asset_count / 13) }}</div>
    {% endif %}
    <div style="border-top:1px solid rgba(0,0,0,0.15); margin:0.25rem 0;"></div>
    <div><strong>Total: {{ "%.1f"|format(scenario_suite.current_grid.token_total) }} tokens</strong></div>
  </div>
</div>
```

**Updated full_migration card** (primary — use white text colors):
```html
<div class="scenario-card primary">
  <span class="sc-label">Full Migration (Universal DDI)</span>
  <span class="hero-tokens" style="color:#fff;">{{ scenario_suite.full_migration.token_total | int }}</span>
  <small style="color:rgba(255,255,255,0.75)">tokens</small>
  <div class="sc-breakdown" style="margin-top:0.75rem; text-align:left; font-size:0.8rem; line-height:1.6; color:rgba(255,255,255,0.9);">
    <div>{{ "{:,}".format(scenario_suite.full_migration.ddi_count) }} DDI ÷ 25 = {{ "%.1f"|format(scenario_suite.full_migration.ddi_count / 25) }}</div>
    <div>{{ "{:,}".format(scenario_suite.full_migration.active_ip_count) }} IPs ÷ 13 = {{ "%.1f"|format(scenario_suite.full_migration.active_ip_count / 13) }}</div>
    {% if scenario_suite.full_migration.asset_count > 0 %}
    <div>{{ "{:,}".format(scenario_suite.full_migration.asset_count) }} Assets ÷ 3 = {{ "%.1f"|format(scenario_suite.full_migration.asset_count / 3) }}</div>
    {% endif %}
    <div style="border-top:1px solid rgba(255,255,255,0.3); margin:0.25rem 0;"></div>
    <div><strong>Total: {{ "%.1f"|format(scenario_suite.full_migration.token_total) }} tokens</strong></div>
  </div>
</div>
```

**Updated hybrid_uddi card** (when `scenario_suite.hybrid_uddi` is not None):

Show two sub-blocks — "NIOS-remaining" and "NIOSX-migrated" — with their own counts/formula, then a combined total:
```html
{% if scenario_suite.hybrid_uddi %}
<div class="scenario-card">
  <span class="sc-label">Hybrid UDDI (NIOS + NIOSX)</span>
  <span class="hero-tokens">{{ scenario_suite.hybrid_uddi.combined_total | int }}</span>
  <small>tokens combined</small>
  <div class="sc-breakdown" style="margin-top:0.75rem; text-align:left; font-size:0.8rem; line-height:1.6;">
    <div style="font-weight:600; margin-bottom:0.15rem; font-size:0.75rem; text-transform:uppercase; letter-spacing:0.04em; opacity:0.7;">NIOS-remaining</div>
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.nios_sub.ddi_count) }} DDI ÷ 50 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.nios_sub.ddi_count / 50) }}</div>
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.nios_sub.active_ip_count) }} IPs ÷ 25 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.nios_sub.active_ip_count / 25) }}</div>
    {% if scenario_suite.hybrid_uddi.nios_sub.asset_count > 0 %}
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.nios_sub.asset_count) }} Assets ÷ 13 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.nios_sub.asset_count / 13) }}</div>
    {% endif %}
    <div style="font-size:0.75rem; opacity:0.75; margin-bottom:0.35rem;">= {{ "%.1f"|format(scenario_suite.hybrid_uddi.nios_sub.token_total) }} tokens</div>

    <div style="font-weight:600; margin-bottom:0.15rem; font-size:0.75rem; text-transform:uppercase; letter-spacing:0.04em; opacity:0.7;">NIOSX-migrated</div>
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.niosx_sub.ddi_count) }} DDI ÷ 25 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.niosx_sub.ddi_count / 25) }}</div>
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.niosx_sub.active_ip_count) }} IPs ÷ 13 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.niosx_sub.active_ip_count / 13) }}</div>
    {% if scenario_suite.hybrid_uddi.niosx_sub.asset_count > 0 %}
    <div>{{ "{:,}".format(scenario_suite.hybrid_uddi.niosx_sub.asset_count) }} Assets ÷ 3 = {{ "%.1f"|format(scenario_suite.hybrid_uddi.niosx_sub.asset_count / 3) }}</div>
    {% endif %}
    <div style="font-size:0.75rem; opacity:0.75; margin-bottom:0.35rem;">= {{ "%.1f"|format(scenario_suite.hybrid_uddi.niosx_sub.token_total) }} tokens</div>

    <div style="border-top:1px solid rgba(0,0,0,0.15); margin:0.25rem 0;"></div>
    <div><strong>Total: {{ "%.1f"|format(scenario_suite.hybrid_uddi.combined_total) }} tokens</strong></div>
  </div>
</div>
{% endif %}
```

The scenario card ordering in the grid must remain: Current Grid → Hybrid UDDI (if present) → Full Migration. This preserves the visual left-to-right ordering that matches cost progression.

**Page reading order** (per CONTEXT.md locked decision):
1. Article header: "NIOS Analysis Complete" (existing)
2. Scenario grid with inline breakdowns (expanded)
3. Download CTA
4. Member Attribution section (Plan 19-02 adds this)
5. "Run Another Analysis" button

Do NOT move the download CTA or the "Run Another Analysis" button — they stay in their current positions relative to the scenario grid.

Write tests in `tests/test_dashboard_nios.py` — add a new `TestScenarioBreakdown` class. Tests use `TestClient` and set `nios_manager` to COMPLETE state with a fabricated `ScenarioSuite`. Assert that the response HTML contains "DDI ÷ 50", "DDI ÷ 25", and comma-formatted numbers. Do NOT rewrite existing tests.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_nios.py::TestScenarioBreakdown -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - complete.html updated with sc-breakdown blocks in all three scenario card variants
    - GET /tab/nios in COMPLETE state (with a ScenarioSuite with non-zero ddi_count) returns HTML containing "DDI ÷ 50" and "DDI ÷ 25"
    - Hybrid card renders NIOS-remaining and NIOSX-migrated sub-blocks when hybrid_uddi is not None
    - Assets row absent when asset_count == 0 (default in current impl)
    - All new TestScenarioBreakdown tests pass
    - All existing tests in test_dashboard_nios.py still pass
  </done>
</task>

</tasks>

<verification>
Run the full NIOS dashboard test suite to confirm no regressions:

```bash
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_nios.py -q 2>&1 | tail -20
```

Expected: all existing tests pass + new TestScenarioBreakdown tests pass.
</verification>

<success_criteria>
- BRKDN-01: Each scenario card shows ddi_count, active_ip_count, asset_count (when > 0) after the hero token number
- BRKDN-02: Each component row displays in the format "N,NNN DDI ÷ 50 = X.X" (comma-separated input, one decimal output)
- Hybrid card has two labeled sub-blocks with separate formulas before the combined total
- complete.html is the only file modified by this plan
- All pre-existing test_dashboard_nios.py tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-token-breakdown-webui/19-01-SUMMARY.md` summarizing what was done, files changed, and test results.
</output>
