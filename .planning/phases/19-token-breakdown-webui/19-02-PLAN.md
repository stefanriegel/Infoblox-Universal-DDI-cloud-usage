---
phase: 19-token-breakdown-webui
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/cloud_usage/dashboard/templates/partials/nios/complete.html
autonomous: false
requirements:
  - BRKDN-03
  - BRKDN-04

must_haves:
  truths:
    - "After NIOS analysis completes, a 'Member Attribution' section is visible below the Download CTA"
    - "The member table has columns: Member, Group, DDI Objects, Active IPs, Token Contribution"
    - "Each row shows the member_hostname, the group label ('NIOS' or 'NIOSX'), and the per-member numeric counts"
    - "When no hybrid split was configured, all rows show 'NIOS' in the Group column"
    - "A subtitle note explains that per-member IP counts are lease-only and should not be summed for scenario totals"
    - "The member table is wrapped in a scrollable container (max-height ~400px, overflow-y auto) so large grids don't overflow the page"
    - "GET /tab/nios in COMPLETE state returns HTML containing 'Member Attribution' and 'NIOS' group label text"
  artifacts:
    - src/cloud_usage/dashboard/templates/partials/nios/complete.html
  key_links:
    - "scenario_suite.member_attribution (List[MemberScenarioRow]) → member table rows"
    - "MemberScenarioRow.group ('nios' | 'niosx') → uppercased to 'NIOS' / 'NIOSX' in template"
---

<objective>
Add a Member Attribution section to `complete.html` below the Download CTA, containing a scrollable table with per-member DDI/IP counts, token contributions, and NIOS/NIOSX group labels.

Purpose: Customers need per-member attribution to audit which members drive which token costs, and to verify their NIOS/NIOSX group assignments are correct. This directly satisfies BRKDN-03 and BRKDN-04.

Output: Modified `complete.html` with a member attribution section added after the Download CTA block. The "Run Another Analysis" button remains at the bottom.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-token-breakdown-webui/19-CONTEXT.md
@.planning/phases/19-token-breakdown-webui/19-01-SUMMARY.md

<interfaces>
<!-- MemberScenarioRow fields available in scenario_suite.member_attribution -->

MemberScenarioRow:
  member_hostname: str     # FQDN of the member
  group: str               # "nios" or "niosx" (lowercase)
  ddi_count: int
  active_ip_count: int     # lease IPs only for this member (NOT deduplicated global total)
  asset_count: int         # always 0 in current implementation
  token_contribution: float

Template access pattern:
  {% for row in scenario_suite.member_attribution %}
    {{ row.member_hostname }}
    {{ row.group | upper }}   {# "NIOS" or "NIOSX" #}
    {{ "{:,}".format(row.ddi_count) }}
    {{ "{:,}".format(row.active_ip_count) }}
    {{ "%.2f"|format(row.token_contribution) }}
  {% endfor %}

<!-- Page structure per CONTEXT.md locked decision -->
Reading order:
  1. Header: "NIOS Analysis Complete" (existing)
  2. Scenario grid with formula breakdowns (added by Plan 19-01)
  3. Download CTA (existing — keep near top)
  4. Member Attribution section  ← INSERT HERE
  5. "Run Another Analysis" button (existing, stays at bottom)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Member Attribution table to complete.html</name>
  <files>src/cloud_usage/dashboard/templates/partials/nios/complete.html</files>
  <behavior>
    - GET /tab/nios in COMPLETE state (with scenario_suite having at least one member in member_attribution) renders HTML containing "Member Attribution"
    - The rendered HTML contains the string "NIOS" as a group label in the table
    - A row with member_hostname "grid-master.example.com" appears in the table when it's in member_attribution
    - When all members have group="nios", no "NIOSX" label appears in the group column
    - When a member has group="niosx", "NIOSX" appears as its group label
    - The subtitle note containing "lease-only" or "per-member" appears in the section
    - The table is wrapped in a div with style containing "overflow-y" (scrollable container)
    - The "Run Another Analysis" button still appears after the member attribution section
  </behavior>
  <action>
Insert the Member Attribution section into `complete.html` between the Download CTA block and the "Run Another Analysis" button paragraph.

**Insert after the `{% if download_filename %}...{% else %}...{% endif %}` block and before the `<p style="margin-top: 1.5rem;">` paragraph containing "Run Another Analysis":**

```html
{% if scenario_suite and scenario_suite.member_attribution %}
<section style="margin-top: 2rem;">
  <p style="margin: 0 0 0.25rem; font-size: 0.85rem; color: var(--ib-gray-600); text-transform: uppercase; letter-spacing: 0.05em;">Member Attribution</p>
  <p style="font-size: 0.8rem; color: var(--ib-gray-600); margin: 0 0 0.75rem;">
    Per-member DDI and IP counts are shown for attribution. Note: per-member Active IP counts are lease-only and will not sum to the scenario total, which uses the globally deduplicated IP count.
  </p>
  <div style="max-height: 400px; overflow-y: auto; border: 1px solid var(--ib-gray-200); border-radius: 6px;">
    <table style="margin: 0; font-size: 0.85rem;">
      <thead>
        <tr>
          <th>Member</th>
          <th>Group</th>
          <th style="text-align:right;">DDI Objects</th>
          <th style="text-align:right;">Active IPs</th>
          <th style="text-align:right;">Token Contribution</th>
        </tr>
      </thead>
      <tbody>
        {% for row in scenario_suite.member_attribution %}
        <tr>
          <td style="font-family: monospace; font-size: 0.8rem;">{{ row.member_hostname }}</td>
          <td>
            <span style="
              font-size: 0.72rem;
              font-weight: 600;
              letter-spacing: 0.04em;
              padding: 0.15rem 0.4rem;
              border-radius: 3px;
              background: {% if row.group == 'niosx' %}var(--ib-blue-light){% else %}var(--ib-gray-200){% endif %};
              color: {% if row.group == 'niosx' %}var(--ib-blue){% else %}var(--ib-gray-600){% endif %};
            ">{{ row.group | upper }}</span>
          </td>
          <td style="text-align:right;">{{ "{:,}".format(row.ddi_count) }}</td>
          <td style="text-align:right;">{{ "{:,}".format(row.active_ip_count) }}</td>
          <td style="text-align:right;">{{ "%.2f"|format(row.token_contribution) }}</td>
        </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</section>
{% endif %}
```

The NIOSX group badge uses `var(--ib-blue-light)` background and `var(--ib-blue)` text to visually distinguish NIOSX members from NIOS members. NIOS rows use the neutral gray palette.

The `{% if scenario_suite and scenario_suite.member_attribution %}` guard means the section is hidden when analysis ran without a scenario_suite (edge case) or when member_attribution is empty.

Write tests in `tests/test_dashboard_nios.py` — add a new `TestMemberAttribution` class. Tests fabricate a `ScenarioSuite` with at least two `MemberScenarioRow` entries (one group="nios", one group="niosx"). Set `nios_manager` to COMPLETE state and assert the response contains "Member Attribution", "NIOS", "NIOSX", member_hostname values, and the overflow-y container. Do NOT rewrite existing tests.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_nios.py::TestMemberAttribution -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Member Attribution section renders in complete.html below the Download CTA when scenario_suite.member_attribution is non-empty
    - Table columns: Member, Group, DDI Objects, Active IPs, Token Contribution
    - Group cell shows "NIOS" or "NIOSX" (uppercased from row.group)
    - Scrollable container with max-height 400px and overflow-y: auto
    - Subtitle note about lease-only per-member IPs is visible
    - "Run Another Analysis" button still appears after the section
    - All new TestMemberAttribution tests pass
    - All existing tests in test_dashboard_nios.py still pass
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Visual verification of complete.html in browser</name>
  <files></files>
  <action>
Start the dashboard server and navigate to the NIOS tab in COMPLETE state to visually verify:

1. Run the server: `python -m cloud_usage.dashboard.app` or `uvicorn cloud_usage.dashboard.app:app --reload` from the repo root (with venv activated)
2. Upload any .tar.gz NIOS backup and run analysis (or manually set the state)
3. After completion, verify in the browser:
   - Each scenario card shows DDI/IP counts and formula rows ("N DDI ÷ 50 = X.X")
   - The formula text is readable (not too small, not clashing with card colors)
   - For primary card (full_migration), formula text is white/light-colored on blue background
   - For hybrid card (if tested): two labeled sub-blocks with "NIOS-remaining" and "NIOSX-migrated"
   - Member Attribution section appears below Download button
   - Table has correct columns and data
   - Scrollbar appears when member_attribution has many rows (max-height 400px enforced)
   - NIOS/NIOSX group labels are visually distinct (badges with color coding)
   - Page reading order: Header → Scenarios → Download → Attribution → Run Another
  </action>
  <verify>Visual inspection in browser</verify>
  <done>All visual elements render correctly. No layout breakage. Formula derivation is self-evident at a glance.</done>
</task>

</tasks>

<verification>
Run the full NIOS dashboard test suite to confirm no regressions:

```bash
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_nios.py -q 2>&1 | tail -30
```

Expected: all tests pass, including TestScenarioBreakdown (from Plan 19-01) and TestMemberAttribution (from this plan).

Also run the broader dashboard test suite to confirm no cross-cutting regressions:
```bash
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_app.py tests/test_dashboard_nios.py -q 2>&1 | tail -20
```
</verification>

<success_criteria>
- BRKDN-03: Member attribution table is visible in the WebUI with columns: Member, Group, DDI Objects, Active IPs, Token Contribution
- BRKDN-04: Each row in the member table shows "NIOS" or "NIOSX" group label derived from MemberScenarioRow.group (uppercased)
- Table renders for all members (no row cap), wrapped in a 400px scrollable container
- When no migration split was configured (all members have group="nios"), table still renders with "NIOS" for all rows
- Section is absent (hidden) when scenario_suite is None (non-complete states)
- Subtitle note clarifying lease-only per-member IPs is present
- All pre-existing and new tests pass
- Visual inspection confirms readable, well-formatted layout
</success_criteria>

<output>
After completion, create `.planning/phases/19-token-breakdown-webui/19-02-SUMMARY.md` summarizing what was done, files changed, and test results.
</output>
