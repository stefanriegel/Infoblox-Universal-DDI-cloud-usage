---
phase: 30-ad-dashboard
plan: 03
type: execute
wave: 1
depends_on: [30-01]
files_modified:
  - src/cloud_usage/dashboard/templates/pages/ad.html
  - src/cloud_usage/dashboard/templates/partials/ad/wizard.html
  - src/cloud_usage/dashboard/templates/partials/ad/progress_display.html
  - src/cloud_usage/dashboard/templates/partials/ad/complete.html
autonomous: true
requirements: [AD-09, AD-10, AD-11, AD-12]

must_haves:
  truths:
    - "pages/ad.html renders wizard in idle state, progress in running state, count tiles in complete state, error + wizard in error state"
    - "wizard.html has all required fields: DC host textarea, auth mode dropdown, domain input, service checkboxes, advanced details block with port/SSL"
    - "Auth mode IIFE toggles username+password fields visible when NTLM, hidden when Kerberos"
    - "Autodiscover checkbox IIFE changes label between Seed server and DC host(s)"
    - "progress_display.html renders indeterminate progress when total==0, determinate when total>0"
    - "complete.html shows three count tiles (DNS Zones, DHCP Scopes, AD Users) + token card + download CTA + Run Another Analysis"
    - "Retry state pre-fills domain field from last_options.domain"
  artifacts:
    - path: "src/cloud_usage/dashboard/templates/pages/ad.html"
      provides: "State-driven AD tab page"
      contains: "ad-progress-container"
    - path: "src/cloud_usage/dashboard/templates/partials/ad/wizard.html"
      provides: "Single-step AD connection form"
      contains: "ad_servers"
    - path: "src/cloud_usage/dashboard/templates/partials/ad/progress_display.html"
      provides: "SSE progress fragment with two-phase bar"
      contains: "progress.total"
    - path: "src/cloud_usage/dashboard/templates/partials/ad/complete.html"
      provides: "Results screen with count tiles and token card"
      contains: "dns_zone_count"
  key_links:
    - from: "src/cloud_usage/dashboard/templates/pages/ad.html"
      to: "/api/sse/ad"
      via: "sse-connect attribute"
      pattern: "sse-connect"
    - from: "src/cloud_usage/dashboard/templates/partials/ad/wizard.html"
      to: "/ad/run"
      via: "hx-post form submission"
      pattern: "hx-post"
    - from: "src/cloud_usage/dashboard/templates/partials/ad/progress_display.html"
      to: "/api/ad/progress"
      via: "hx-get on sse:ad_progress"
      pattern: "hx-get"
---

<objective>
Create all four AD dashboard Jinja2 templates. This plan runs in Wave 1 parallel with Plan 02 (no file conflicts — templates and Python source are entirely separate files). Templates mirror the NIOS tab pattern with AD-specific form fields, two-phase progress bar, and count tile results screen.

Purpose: After this plan, the template layer is complete and Plan 04 can wire everything together.
Output: Four new template files under dashboard/templates/.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-ad-dashboard/30-CONTEXT.md
@.planning/phases/30-ad-dashboard/30-RESEARCH.md

<interfaces>
<!-- Source template: src/cloud_usage/dashboard/templates/pages/nios.html -->
<!-- State-driven conditional: nios_state == 'running'|'complete'|'error'|idle -->
<!-- AD equivalent uses: ad_state variable -->

<!-- Source: src/cloud_usage/dashboard/templates/partials/nios/complete.html -->
<!-- Count tile pattern: div.scenario-grid > div.scenario-card with sc-label + hero-tokens spans -->
<!-- Download CTA: a href="/download/{{ download_filename }}" role="button" download="..." -->
<!-- Run Another Analysis: button hx-get="/tab/nios" hx-target="#tab-container" -->

<!-- LOCKED DECISIONS from CONTEXT.md (must implement exactly): -->
<!-- wizard: single-step form, all fields in one article -->
<!-- auth_mode dropdown: Kerberos / NTLM — IIFE toggles cred fields on change -->
<!-- autodiscover checkbox: IIFE changes label between "Seed server" and "DC host(s)" -->
<!-- services: three checkboxes (DNS, DHCP, Users), all checked by default -->
<!-- port=5985, SSL unchecked, both in details advanced block -->
<!-- error state: shows message + wizard pre-filled via last_options template variables -->
<!-- results: three count tiles + token card + download CTA + Run Another Analysis -->

<!-- Template variables provided by tab_ad() route (from Plan 04): -->
<!-- ad_state: str, dns_zone_count: int, dhcp_scope_count: int, user_count: int -->
<!-- ddi_count: int, ip_count: int, token_total: float -->
<!-- download_filename: str|None, error: str|None, last_options: AdOptions|None -->
<!-- active_tab: str, scan_state: str, nios_state: str (from _get_tab_context) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pages/ad.html and partials/ad/progress_display.html</name>
  <files>
    src/cloud_usage/dashboard/templates/pages/ad.html
    src/cloud_usage/dashboard/templates/partials/ad/progress_display.html
  </files>
  <action>
    Create the partials/ad/ subdirectory implicitly by creating files within it.

    CREATE src/cloud_usage/dashboard/templates/pages/ad.html:

    Mirror pages/nios.html. Replace nios_state with ad_state, SSE events with ad_* equivalents.
    State conditions: running -> progress, complete -> include complete.html, error -> error msg + wizard, idle -> wizard.

    Key SSE wiring in running state:
    - Outer div: hx-ext="sse" sse-connect="/api/sse/ad"
    - Inner progress area div: hx-get="/api/ad/progress" hx-trigger="sse:ad_progress" hx-swap="innerHTML"
    - Completion trigger div: hx-trigger="sse:ad_complete" hx-get="/tab/ad" hx-target="#tab-container"

    File content (write verbatim):

    {# AD Analysis tab -- full #tab-container with tab bar + content.
       State-driven: idle shows wizard, running shows SSE progress,
       complete shows count tiles + token card + download, error shows error + wizard.
       Variables: active_tab, ad_state (str), scan_state (str), nios_state (str),
       dns_zone_count, dhcp_scope_count, user_count, ddi_count, ip_count, token_total,
       download_filename (str|None), error (str|None), last_options (AdOptions|None) #}

    Idle and error states both render partials/ad/wizard.html (error state gets the same
    form but with last_options values available as template context).

    CREATE src/cloud_usage/dashboard/templates/partials/ad/progress_display.html:

    Two-phase progress bar. When progress.total == 0: indeterminate spinner (just bare
    `&lt;progress&gt;` with no attributes). When progress.total > 0: determinate bar.
    Always show elapsed time and label.

    Exact file content:
    {# AD pipeline progress display fragment.
       Fetched by HTMX GET /api/ad/progress on each sse:ad_progress event.
       Swapped into #ad-progress-area in pages/ad.html.
       progress.total == 0 = indeterminate (autodiscovery); > 0 = determinate. #}
    {% if progress.total > 0 %}
      &lt;progress value="{{ progress.step }}" max="{{ progress.total }}"&gt;&lt;/progress&gt;
      &lt;p&gt;Step {{ progress.step }} of {{ progress.total }}: {{ progress.label }}&lt;/p&gt;
    {% else %}
      &lt;progress&gt;&lt;/progress&gt;
      &lt;p&gt;{{ progress.label }}&lt;/p&gt;
    {% endif %}
    &lt;p&gt;Elapsed: {{ progress.elapsed_seconds }}s&lt;/p&gt;

    Note: write actual HTML tags (angle brackets), not HTML entities. The entities above
    are used here only to avoid Jinja2 interpretation in this plan document.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -c "import os; assert os.path.exists('src/cloud_usage/dashboard/templates/pages/ad.html'); assert os.path.exists('src/cloud_usage/dashboard/templates/partials/ad/progress_display.html'); print('files exist OK')"</automated>
  </verify>
  <done>Both files exist. pages/ad.html contains sse-connect="/api/sse/ad" in running state block. progress_display.html contains both progress.total conditional branches and elapsed_seconds display.</done>
</task>

<task type="auto">
  <name>Task 2: Create partials/ad/wizard.html and partials/ad/complete.html</name>
  <files>
    src/cloud_usage/dashboard/templates/partials/ad/wizard.html
    src/cloud_usage/dashboard/templates/partials/ad/complete.html
  </files>
  <action>
    CREATE src/cloud_usage/dashboard/templates/partials/ad/wizard.html:

    Single-step form. All fields in one article. hx-post="/ad/run" hx-target="#tab-content".

    Field list (in order):
    1. "Auto-discover DCs from forest" checkbox (id="ad-autodiscover") — controls label IIFE
    2. DC host(s) / Seed server textarea (name="ad_servers", id="ad-servers-input") with label id="ad-dc-label" and help text id="ad-dc-help". Default label "DC host(s)". HTML5 required. Pre-fill: value="{{ last_options.servers | join(', ') if last_options else '' }}"
    3. Domain input (name="ad_domain") — always required. Pre-fill: value="{{ last_options.domain if last_options else '' }}"
    4. Auth mode select (name="ad_auth_mode", id="ad-auth-mode") — options: value="kerberos" (selected by default) and value="ntlm". Pre-fill: selected="{{ 'selected' if last_options and last_options.auth_mode == 'ntlm' else '' }}" on ntlm option
    5. Credential fields div (id="ad-cred-fields", style="display:none" by default):
       - Username input (name="ad_username") — NOT pre-filled (security: never re-show password)
       - Password input type="password" (name="ad_password")
    6. Services checkboxes section:
       - ad_svc_dns checkbox, checked by default, label "DNS"
       - ad_svc_dhcp checkbox, checked by default, label "DHCP"
       - ad_svc_user checkbox, checked by default, label "AD Users"
    7. Advanced settings details block (collapsed by default):
       - Port input (name="ad_port", value="{{ last_options.winrm_port if last_options else 5985 }}", type="number")
       - SSL checkbox (name="ad_ssl") — unchecked by default

    Submit button: hx-post="/ad/run" on the form OR as an hx-include button. Use standard form approach: form action="" hx-post="/ad/run" hx-target="#tab-content".

    IIFE 1 — Auth mode toggle (runs immediately on load for retry pre-fill):
    (function() {
      var authMode = document.getElementById('ad-auth-mode');
      var credFields = document.getElementById('ad-cred-fields');
      function updateFields() {
        credFields.style.display = authMode.value === 'ntlm' ? '' : 'none';
      }
      authMode.addEventListener('change', updateFields);
      updateFields();
    })();

    IIFE 2 — Autodiscover label toggle:
    (function() {
      var autodiscover = document.getElementById('ad-autodiscover');
      var dcLabel = document.getElementById('ad-dc-label');
      var dcHelp = document.getElementById('ad-dc-help');
      function updateLabel() {
        if (autodiscover.checked) {
          dcLabel.textContent = 'Seed server';
          dcHelp.textContent = 'Single DC hostname — forest DCs discovered via Get-ADForest';
        } else {
          dcLabel.textContent = 'DC host(s)';
          dcHelp.textContent = 'Comma-separated list of Domain Controller hostnames';
        }
      }
      autodiscover.addEventListener('change', updateLabel);
      updateLabel();
    })();

    Autodiscover checked pre-fill: {{ 'checked' if last_options and last_options.autodiscover else '' }}
    Discovery server pre-fill in textarea when autodiscover: value="{{ last_options.discovery_server if last_options and last_options.autodiscover else '' }}"
    For non-autodiscover mode: textarea value="{{ last_options.servers | join(', ') if last_options and not last_options.autodiscover else '' }}"

    CREATE src/cloud_usage/dashboard/templates/partials/ad/complete.html:

    Mirror nios/complete.html structure but with AD-specific content.

    Three count tiles in a flex row (use scenario-grid class from existing CSS):
    - DNS Zones tile: sc-label="DNS Zones", hero-tokens="{{ dns_zone_count }}", small="zones"
    - DHCP Scopes tile: sc-label="DHCP Scopes", hero-tokens="{{ dhcp_scope_count }}", small="scopes"
    - AD Users tile: sc-label="AD Users", hero-tokens="{{ user_count }}", small="users"

    Token card (primary scenario-card):
    - Show DDI line if ddi_count > 0: "{{ '{:,}'.format(ddi_count) }} DDI ÷ 25 = {{ '%.1f'|format(ddi_count / 25) }}"
    - Show IPs line if ip_count > 0: "{{ '{:,}'.format(ip_count) }} IPs ÷ 13 = {{ '%.1f'|format(ip_count / 13) }}"
    - Divider line + Total: "{{ '%.1f'|format(token_total) }} tokens"

    Download CTA (mirror nios/complete.html pattern):
    {% if download_filename %}
      a href="/download/{{ download_filename }}" role="button" download="{{ download_filename }}"
      Download XLS Report
      small: {{ download_filename }}
    {% else %}
      em: Output file not available.
    {% endif %}

    Run Another Analysis button:
    button.secondary.outline hx-get="/tab/ad" hx-target="#tab-container"
    Text: "Run Another Analysis"

    Wrap everything in an article.completion-banner with header "AD Analysis Complete".
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -c "import os; assert os.path.exists('src/cloud_usage/dashboard/templates/partials/ad/wizard.html'); assert os.path.exists('src/cloud_usage/dashboard/templates/partials/ad/complete.html'); print('files exist OK')"</automated>
  </verify>
  <done>Both files exist. wizard.html contains ad-auth-mode, ad-autodiscover, ad_servers, ad_domain, ad_svc_dns fields and both IIFE script blocks. complete.html contains dns_zone_count, dhcp_scope_count, user_count, ddi_count, token_total template variables and a Run Another Analysis button pointing to /tab/ad.</done>
</task>

</tasks>

<verification>
- All four template files exist in correct paths
- pages/ad.html: contains sse-connect="/api/sse/ad", ad_complete SSE trigger, all four state branches (idle/running/complete/error)
- wizard.html: contains both IIFE script blocks, all required form fields, hx-post="/ad/run"
- progress_display.html: contains progress.total conditional for two-phase bar
- complete.html: contains dns_zone_count, dhcp_scope_count, user_count tiles and token_total card
</verification>

<success_criteria>
All four template files exist with correct content. Jinja2 can parse all four without syntax errors (tested via Environment.get_template). Plans 02 and 03 together provide all backend and template components needed for Plan 04 to wire up.
</success_criteria>

<output>
After completion, create .planning/phases/30-ad-dashboard/30-03-SUMMARY.md
</output>
