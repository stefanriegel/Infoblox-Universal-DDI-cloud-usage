---
phase: 30-ad-dashboard
plan: 05
type: execute
wave: 3
depends_on: [30-04]
files_modified:
  - src/cloud_usage/dashboard/templates/partials/ad/wizard.html
  - tests/test_dashboard_ad.py
autonomous: true
gap_closure: true
requirements: [AD-09, AD-12]

must_haves:
  truths:
    - "POST /ad/run with wizard's actual field names (no ad_ prefix for servers/auth_mode/autodiscover/ssl/port) builds AdOptions with the correct servers list"
    - "GET /tab/ad in error state shows pre-filled servers without referencing a missing domain field"
    - "Tests post field names that match what the wizard actually sends, so test coverage mirrors real browser behavior"
  artifacts:
    - path: "src/cloud_usage/dashboard/templates/partials/ad/wizard.html"
      provides: "Form fields aligned to route handler names"
      contains: "name=\"servers\""
    - path: "tests/test_dashboard_ad.py"
      provides: "Integration test asserting wizard field names reach the route correctly"
      contains: "test_ad_run_wizard_field_names"
  key_links:
    - from: "src/cloud_usage/dashboard/templates/partials/ad/wizard.html"
      to: "src/cloud_usage/dashboard/routes/ad.py"
      via: "hx-post form field names"
      pattern: "name=\"servers\""
---

<objective>
Close two verified gaps in the Phase 30 AD dashboard:

1. (Blocker, AD-09) wizard.html sends five form fields with an `ad_` prefix that the POST /ad/run route does not read — real browser submissions always produce an empty `servers` list. Fix: rename the five mismatched fields in wizard.html to drop the `ad_` prefix, aligning them with what ad.py reads. Also add an integration test that posts the wizard's actual field names.

2. (Warning, AD-12) wizard.html references `last_options.domain` on line 35, but AdOptions has no `domain` attribute — Jinja2 silently renders empty string on error retry. Fix: remove the domain pre-fill expression from that input's value attribute.

Purpose: After this plan, a real browser form submission from the AD wizard will build a correct AdOptions (non-empty servers list, correct auth_mode, port, ssl, autodiscover) and the error-state retry wizard will not reference a non-existent field.
Output: Two files modified — wizard.html (field name renames + domain pre-fill removal) and test_dashboard_ad.py (new integration test + existing test alignment).
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-ad-dashboard/30-CONTEXT.md
@.planning/phases/30-ad-dashboard/30-VERIFICATION.md

<interfaces>
<!-- ad.py POST /ad/run reads these form field names (lines 174–189): -->
<!--   form.get("ad_username")          → username (already aligned — keep as-is) -->
<!--   form.get("ad_password")          → password (already aligned — keep as-is) -->
<!--   form.get("auth_mode", "kerberos")  → auth_mode (MISMATCHED — wizard sends ad_auth_mode) -->
<!--   form.get("servers", "")           → servers (MISMATCHED — wizard sends ad_servers) -->
<!--   form.get("winrm_port", "5985")    → winrm_port (MISMATCHED — wizard sends ad_port) -->
<!--   form.get("winrm_ssl")             → winrm_ssl (MISMATCHED — wizard sends ad_ssl) -->
<!--   form.get("autodiscover")          → autodiscover (MISMATCHED — wizard sends ad_autodiscover) -->
<!--   form.get("discovery_server")      → discovery_server (not in wizard form — leave as-is) -->
<!--   form.get("ad_svc_dns")            → svc_dns (already aligned — keep as-is) -->
<!--   form.get("ad_svc_dhcp")           → svc_dhcp (already aligned — keep as-is) -->
<!--   form.get("ad_svc_user")           → svc_user (already aligned — keep as-is) -->

<!-- wizard.html current field names that MUST change: -->
<!--   name="ad_servers"      → name="servers"      (textarea, line 22) -->
<!--   name="ad_auth_mode"    → name="auth_mode"    (select, line 39) -->
<!--   name="ad_autodiscover" → name="autodiscover" (checkbox, line 14) -->
<!--   name="ad_ssl"          → name="winrm_ssl"    (checkbox, line 90) -->
<!--   name="ad_port"         → name="winrm_port"   (number input, line 82) -->

<!-- wizard.html domain pre-fill to REMOVE: -->
<!--   Line 35: value="{{ last_options.domain if last_options else '' }}" -->
<!--   AdOptions has no .domain field — confirmed in options.py dataclass -->
<!--   Fix: change to value="" (remove the Jinja2 expression entirely) -->

<!-- Fields that are ALREADY correctly aligned (do not touch): -->
<!--   name="ad_username", name="ad_password" -->
<!--   name="ad_svc_dns", name="ad_svc_dhcp", name="ad_svc_user" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix wizard.html field names and domain pre-fill</name>
  <files>src/cloud_usage/dashboard/templates/partials/ad/wizard.html</files>
  <behavior>
    - After fix: submitting the form with servers="dc1.corp.com" sends field name "servers" (not "ad_servers") to POST /ad/run
    - After fix: auth_mode select sends field name "auth_mode" (not "ad_auth_mode")
    - After fix: autodiscover checkbox sends field name "autodiscover" (not "ad_autodiscover")
    - After fix: SSL checkbox sends field name "winrm_ssl" (not "ad_ssl")
    - After fix: port input sends field name "winrm_port" (not "ad_port")
    - After fix: domain input has no Jinja2 expression in value attribute — renders as value=""
    - Fields that must NOT change: ad_username, ad_password, ad_svc_dns, ad_svc_dhcp, ad_svc_user
  </behavior>
  <action>
    READ the file before editing.

    Make exactly these six targeted replacements (do not rewrite the file):

    1. Line 14 — autodiscover checkbox:
       Change: name="ad_autodiscover"
       To:     name="autodiscover"

    2. Line 22 — servers textarea:
       Change: name="ad_servers"
       To:     name="servers"

    3. Line 35 — domain input value attribute:
       Change: value="{{ last_options.domain if last_options else '' }}"
       To:     value=""

    4. Line 39 — auth_mode select:
       Change: name="ad_auth_mode"
       To:     name="auth_mode"

    5. Line 82 — WinRM port input:
       Change: name="ad_port"
       To:     name="winrm_port"

    6. Line 90 — SSL checkbox:
       Change: name="ad_ssl"
       To:     name="winrm_ssl"

    After editing, verify the five renamed fields and confirm ad_username, ad_password, ad_svc_dns, ad_svc_dhcp, ad_svc_user are unchanged.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -c "
content = open('src/cloud_usage/dashboard/templates/partials/ad/wizard.html').read()
# Fields that must be present with new names
assert 'name=\"servers\"' in content, 'MISSING: name=servers'
assert 'name=\"auth_mode\"' in content, 'MISSING: name=auth_mode'
assert 'name=\"autodiscover\"' in content, 'MISSING: name=autodiscover'
assert 'name=\"winrm_ssl\"' in content, 'MISSING: name=winrm_ssl'
assert 'name=\"winrm_port\"' in content, 'MISSING: name=winrm_port'
# Old prefixed names must be gone (except the ones that stay)
assert 'name=\"ad_servers\"' not in content, 'STILL PRESENT: name=ad_servers'
assert 'name=\"ad_auth_mode\"' not in content, 'STILL PRESENT: name=ad_auth_mode'
assert 'name=\"ad_autodiscover\"' not in content, 'STILL PRESENT: name=ad_autodiscover'
assert 'name=\"ad_ssl\"' not in content, 'STILL PRESENT: name=ad_ssl'
assert 'name=\"ad_port\"' not in content, 'STILL PRESENT: name=ad_port'
# Domain pre-fill removed
assert 'last_options.domain' not in content, 'STILL PRESENT: last_options.domain'
# Fields that must remain unchanged
assert 'name=\"ad_username\"' in content, 'MISSING: name=ad_username'
assert 'name=\"ad_password\"' in content, 'MISSING: name=ad_password'
assert 'name=\"ad_svc_dns\"' in content, 'MISSING: name=ad_svc_dns'
assert 'name=\"ad_svc_dhcp\"' in content, 'MISSING: name=ad_svc_dhcp'
assert 'name=\"ad_svc_user\"' in content, 'MISSING: name=ad_svc_user'
print('wizard.html field names OK')
"
    </automated>
  </verify>
  <done>wizard.html has five field names corrected (servers, auth_mode, autodiscover, winrm_ssl, winrm_port), the domain pre-fill expression is removed, and all five previously-correct fields (ad_username, ad_password, ad_svc_dns, ad_svc_dhcp, ad_svc_user) remain unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration test for wizard field names and fix domain pre-fill test</name>
  <files>tests/test_dashboard_ad.py</files>
  <behavior>
    - New test `test_ad_run_wizard_field_names`: POST /ad/run using the wizard's actual field names (servers, auth_mode, winrm_port, winrm_ssl, autodiscover — after the fix) and assert status 200 and that AdOptions.servers is non-empty
    - Existing `test_tab_ad_error_prefills_wizard`: currently asserts `dc1.corp.com` in r.text (from servers pre-fill) — this assertion is still valid after the fix; confirm it still passes, no change needed
    - No test should assert on `last_options.domain` appearing in rendered HTML (domain pre-fill is removed)
  </behavior>
  <action>
    READ the file before editing.

    In the `TestAdRun` class, add a new test method immediately after `test_ad_run_kerberos_omits_credentials`:

    ```python
    def test_ad_run_wizard_field_names(self) -> None:
        """POST /ad/run with wizard's actual field names (after fix) builds correct AdOptions.

        Regression test for Gap 1 (AD-09): wizard.html previously sent ad_servers,
        ad_auth_mode, etc. with an ad_ prefix that the route did not read.
        After the fix, field names match what ad.py reads.
        """
        captured = {}

        def fake_pipeline(ad_mgr, opts, event_bridge):
            captured["opts"] = opts

        with mock.patch(
            "cloud_usage.dashboard.routes.ad._run_ad_pipeline",
            side_effect=fake_pipeline,
        ):
            with TestClient(create_app()) as client:
                r = client.post(
                    "/ad/run",
                    data={
                        "servers": "dc1.corp.com",
                        "auth_mode": "kerberos",
                        "winrm_port": "5985",
                        "winrm_ssl": "",
                        "autodiscover": "",
                        "ad_svc_dns": "on",
                        "ad_svc_dhcp": "on",
                        "ad_svc_user": "on",
                    },
                )
                assert r.status_code == 200
                import time; time.sleep(0.05)

        assert "opts" in captured, "Pipeline was not called"
        opts = captured["opts"]
        assert opts.servers == ["dc1.corp.com"], (
            f"Expected servers=['dc1.corp.com'], got {opts.servers!r}. "
            "Field name mismatch between wizard.html and ad.py POST handler."
        )
        assert opts.auth_mode == "kerberos"
    ```

    Do NOT modify any other existing tests. The existing `test_tab_ad_error_prefills_wizard` (line 239) only asserts `dc1.corp.com` in `r.text` via the servers textarea pre-fill — this remains valid and requires no change.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_ad.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All existing tests in test_dashboard_ad.py continue to pass. The new `test_ad_run_wizard_field_names` test passes, confirming that posting the wizard's corrected field names to POST /ad/run builds an AdOptions with a non-empty servers list.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full test suite to confirm no regressions:

```
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage
python -m pytest tests/test_dashboard_ad.py -v
python -m pytest tests/ -x -q -m "not integration"
```

Verify wizard.html field name alignment:
```
grep -n 'name=' src/cloud_usage/dashboard/templates/partials/ad/wizard.html
```
Expected output must show: servers, auth_mode, autodiscover, winrm_ssl, winrm_port (no ad_ prefix on these five), while ad_username, ad_password, ad_svc_dns, ad_svc_dhcp, ad_svc_user retain their prefix.

Verify domain pre-fill removed:
```
grep -n 'domain' src/cloud_usage/dashboard/templates/partials/ad/wizard.html
```
Must NOT contain `last_options.domain`.
</verification>

<success_criteria>
- pytest tests/test_dashboard_ad.py -v shows all tests green including the new test_ad_run_wizard_field_names
- pytest tests/ -x -q -m "not integration" shows no regressions across the full suite
- wizard.html grep for name= shows corrected field names (servers, auth_mode, autodiscover, winrm_ssl, winrm_port) with no ad_ prefix on these five
- wizard.html contains no reference to last_options.domain
- Human verification: navigate to AD Analysis tab in a running browser session, enter a DC hostname, click Run AD Analysis — the form submission triggers the running state (SSE spinner), confirming servers field is non-empty

Human browser check (blocking):
1. Run: `uvicorn cloud_usage.dashboard.app:create_app --factory --reload`
2. Navigate to http://localhost:8000 → AD Analysis tab
3. Enter "dc1.corp.com" in DC host(s), leave auth as Kerberos, click Run AD Analysis
4. Confirm: running state appears with SSE progress spinner (not a blank page or no-op)
5. Confirm: no AttributeError in server logs related to domain field
</success_criteria>

<output>
After completion, create .planning/phases/30-ad-dashboard/30-05-SUMMARY.md
</output>
