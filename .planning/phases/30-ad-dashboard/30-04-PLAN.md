---
phase: 30-ad-dashboard
plan: 04
type: execute
wave: 2
depends_on: [30-02, 30-03]
files_modified:
  - src/cloud_usage/dashboard/app.py
  - src/cloud_usage/dashboard/routes/pages.py
  - src/cloud_usage/dashboard/templates/partials/tab_bar.html
autonomous: true
requirements: [AD-09, AD-10, AD-11, AD-12]

must_haves:
  truths:
    - "GET /tab/ad returns 200 with AD wizard in idle state"
    - "app.state.ad_manager and app.state.ad_event_bridge exist after lifespan startup"
    - "ad_state appears in all tab context renders so tab_bar.html AD badge works"
    - "AD Analysis tab appears in the tab bar on every page"
    - "Tab bar AD badge shows running/done/error states"
    - "GET /tab/ad in complete state shows dns_zone_count, dhcp_scope_count, user_count"
    - "GET /tab/ad in error state shows error message and pre-filled wizard"
    - "ad_manager and nios_manager are independent objects on app.state"
    - "ad_event_bridge and nios_event_bridge are independent EventBridge instances"
  artifacts:
    - path: "src/cloud_usage/dashboard/app.py"
      provides: "ad_event_bridge + AdScanManager in lifespan; ad_router included"
      contains: "ad_event_bridge"
    - path: "src/cloud_usage/dashboard/routes/pages.py"
      provides: "ad_state in _get_tab_context(); tab_ad() route at GET /tab/ad"
      contains: "ad_state"
    - path: "src/cloud_usage/dashboard/templates/partials/tab_bar.html"
      provides: "AD Analysis tab li with badge"
      contains: "AD Analysis"
  key_links:
    - from: "src/cloud_usage/dashboard/app.py"
      to: "src/cloud_usage/dashboard/services/ad_manager.py"
      via: "AdScanManager() stored as app.state.ad_manager"
      pattern: "ad_manager"
    - from: "src/cloud_usage/dashboard/app.py"
      to: "src/cloud_usage/dashboard/routes/ad.py"
      via: "app.include_router(ad_router)"
      pattern: "ad_router"
    - from: "src/cloud_usage/dashboard/routes/pages.py"
      to: "src/cloud_usage/dashboard/services/ad_manager.py"
      via: "request.app.state.ad_manager.state.value"
      pattern: "ad_state"
    - from: "src/cloud_usage/dashboard/templates/partials/tab_bar.html"
      to: "/tab/ad"
      via: "hx-get link"
      pattern: "hx-get"
---

<objective>
Wire the AD tab into the running application. Edits three existing files: registers AdScanManager and ad_event_bridge in app.py lifespan, adds ad_state to _get_tab_context() and adds tab_ad() route in pages.py, and adds the AD Analysis tab entry to tab_bar.html.

Purpose: After this plan, GET /tab/ad returns 200 and all Phase 30 tests pass.
Output: Three edited existing files — no new files.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-ad-dashboard/30-CONTEXT.md
@.planning/phases/30-ad-dashboard/30-RESEARCH.md

<interfaces>
<!-- Current app.py lifespan startup (read file before editing): -->
<!-- Startup block adds: event_bridge, scan_manager, nios_event_bridge, nios_manager, templates -->
<!-- Shutdown block closes: event_bridge, nios_event_bridge -->
<!-- create_app() includes: download_router, pages_router, partials_router, scan_router, sse_router, nios_router -->

<!-- Current _get_tab_context() return dict keys: -->
<!-- request, active_tab, scan_state, resource_count, providers, total_resources, total_providers, nios_state -->
<!-- MUST ADD: ad_state -->

<!-- Current tab_bar.html ends with NIOS Analysis li entry -->
<!-- ADD AD Analysis li after NIOS li -->

<!-- Critical pitfall (RESEARCH.md Pitfall 4): -->
<!-- ad_state MUST be added to _get_tab_context() (not just tab_ad()) -->
<!-- because every tab route calls _get_tab_context() and tab_bar.html -->
<!-- will reference ad_state on every page render after this edit -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit app.py and pages.py</name>
  <files>
    src/cloud_usage/dashboard/app.py
    src/cloud_usage/dashboard/routes/pages.py
  </files>
  <action>
    READ both files before editing.

    EDIT src/cloud_usage/dashboard/app.py:

    Add two imports after the existing NiosScanManager import line:
      from cloud_usage.dashboard.services.ad_manager import AdScanManager
      from cloud_usage.dashboard.routes.ad import router as ad_router

    In lifespan() startup block, insert after the nios_manager line and before app.state.templates:
      # AD analysis state -- independent of cloud scan and NIOS (SC-5)
      ad_event_bridge = EventBridge()
      await ad_event_bridge.start()
      app.state.ad_event_bridge = ad_event_bridge
      ad_manager = AdScanManager()
      app.state.ad_manager = ad_manager

    In lifespan() shutdown block, add after the nios_event_bridge.close() line:
      await ad_event_bridge.close()

    In create_app(), add after app.include_router(nios_router):
      app.include_router(ad_router)

    EDIT src/cloud_usage/dashboard/routes/pages.py:

    In _get_tab_context(), add after the nios_state lines (before the return statement):
      ad_manager = request.app.state.ad_manager
      ad_state = ad_manager.state.value

    Add "ad_state": ad_state to the return dict alongside "nios_state": nios_state.

    Add tab_ad() route function after tab_nios(). The function docstring, args, and return follow the same pattern as tab_nios(). Key implementation:
      - Read ad_manager from request.app.state.ad_manager
      - Read templates from request.app.state.templates
      - Compute download_filename = os.path.basename(ad_manager.output_path) if ad_manager.output_path else None
      - Build context via _get_tab_context(request, "ad")
      - context.update with: ad_state, dns_zone_count, dhcp_scope_count, user_count, ddi_count, ip_count, token_total, download_filename, error, last_options
      - Return templates.TemplateResponse(request, "pages/ad.html", context)
      - Import os inline (inside the function body, mirrors tab_nios pattern)
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -c "from cloud_usage.dashboard.app import create_app; from fastapi.testclient import TestClient; app = create_app(); client = TestClient(app); client.__enter__(); resp = client.get('/tab/ad'); print('status:', resp.status_code); assert resp.status_code == 200; client.__exit__(None,None,None); print('GET /tab/ad OK')"  </verify>
  <done>GET /tab/ad returns 200 HTML. app.state.ad_manager exists. app.state.ad_event_bridge exists and is not the same object as app.state.nios_event_bridge.</done>
</task>

<task type="auto">
  <name>Task 2: Add AD tab entry to tab_bar.html</name>
  <files>src/cloud_usage/dashboard/templates/partials/tab_bar.html</files>
  <action>
    READ the current tab_bar.html before editing. The file ends with the NIOS Analysis li entry.

    Add an AD Analysis li entry immediately after the closing NIOS Analysis li tag, before the closing ul tag:

    The new li mirrors the NIOS entry exactly with ad-specific names:
    - hx-get="/tab/ad"
    - role="tab" aria-selected="{{ 'true' if active_tab == 'ad' else 'false' }}"
    - class="{{ 'tab-active' if active_tab == 'ad' else '' }}"
    - Tab label: "AD Analysis"
    - Badge logic:
      if ad_state == 'running': badge-warning "running..."
      elif ad_state == 'complete': badge-success "done"
      elif ad_state == 'error': badge-error "error"
      (no badge in idle state)
  </action>
  <verify>
    <automated>python -c "
content = open('src/cloud_usage/dashboard/templates/partials/tab_bar.html').read()
assert 'AD Analysis' in content, 'Missing AD Analysis tab entry'
assert '/tab/ad' in content, 'Missing /tab/ad hx-get'
assert 'ad_state' in content, 'Missing ad_state badge logic'
print('tab_bar.html AD tab OK')
"</automated>
  </verify>
  <done>tab_bar.html contains the AD Analysis tab li with hx-get="/tab/ad", active-tab conditional class, and three-state badge logic (running/done/error).</done>
</task>

</tasks>

<verification>
- pytest tests/test_dashboard_ad.py -x -q — all tests green
- pytest tests/ -x -q -m "not integration" — full suite green (no regressions on existing tabs)
- GET /tab/ad in idle state: 200, wizard form visible
- GET /tab/nios still works (regression check): 200
- app.state.ad_manager is not app.state.nios_manager (independence)
- app.state.ad_event_bridge is not app.state.nios_event_bridge (independence)
</verification>

<success_criteria>
pytest tests/test_dashboard_ad.py -x -q passes all tests. pytest tests/ -x -q -m "not integration" passes with no regressions. The AD Analysis tab appears in the browser tab bar and GET /tab/ad returns the wizard form in idle state.
</success_criteria>

<output>
After completion, create .planning/phases/30-ad-dashboard/30-04-SUMMARY.md
</output>
