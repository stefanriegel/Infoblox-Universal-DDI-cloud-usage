---
phase: 08-dashboard-gcp-scan-fix
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - tests/test_dashboard_scan.py
  - tests/test_dashboard_wizard.py
autonomous: true
requirements:
  - DISC-02
  - DISC-03
  - DISC-07
  - PLAT-02

must_haves:
  truths:
    - "Unit tests for _enumerate_accounts verify correct call signatures for all 3 providers"
    - "Unit tests for _build_discovery_providers verify correct GCP 6-arg call and Azure/AWS calls"
    - "Error path tests verify inline error message is returned when enumeration fails"
    - "All-providers-fail test verifies the blocking message is rendered in the wizard"
    - "CLI-vs-dashboard parity test proves both code paths produce the same ProjectInfo list"
    - "Azure display_name fallback test confirms the id fallback works when display_name is missing"
  artifacts:
    - path: "tests/test_dashboard_scan.py"
      provides: "Unit tests for _build_discovery_providers correct call signatures and _enumerate_accounts GCP/Azure fixes"
      contains: "test_enumerate.*gcp.*six_args"
    - path: "tests/test_dashboard_wizard.py"
      provides: "Wizard error UX tests and CLI-vs-dashboard parity test"
      contains: "test_.*error.*inline"
  key_links:
    - from: "tests/test_dashboard_scan.py"
      to: "cloud_usage.dashboard.routes.scan._enumerate_accounts"
      via: "mock.patch at provider function level"
      pattern: "mock\\.patch.*enumerate_gcp_projects"
    - from: "tests/test_dashboard_scan.py"
      to: "cloud_usage.dashboard.routes.scan._build_discovery_providers"
      via: "mock.patch at provider function level"
      pattern: "mock\\.patch.*enumerate_gcp_projects"
---

<objective>
Add regression tests covering all 4 bug fixes, error UX paths, and CLI-vs-dashboard parity for the dashboard scan wizard.

Purpose: Prevent regression of the call-signature, type-iteration, and dict-key bugs. Verify the error UX works for all providers. Prove dashboard and CLI paths produce identical results.
Output: Comprehensive test coverage for _enumerate_accounts, _build_discovery_providers, error paths, and parity.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-dashboard-gcp-scan-fix/08-CONTEXT.md
@.planning/phases/08-dashboard-gcp-scan-fix/08-RESEARCH.md
@.planning/phases/08-dashboard-gcp-scan-fix/08-01-SUMMARY.md
@src/cloud_usage/dashboard/routes/scan.py
@src/cloud_usage/providers/gcp/projects.py
@tests/test_dashboard_scan.py
@tests/test_dashboard_wizard.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _enumerate_accounts and _build_discovery_providers regression tests</name>
  <files>tests/test_dashboard_scan.py</files>
  <action>
Add new test classes to `tests/test_dashboard_scan.py` covering the 4 fixed bugs. Follow the existing test patterns in this file: sys.modules injection for cloud SDKs, pre-import before mock.patch, realistic mock data.

**Create shared helper fixtures at module level:**

```python
from cloud_usage.providers.gcp.projects import ProjectInfo

def _make_project_info(project_id="proj-1", **flags):
    return ProjectInfo(
        project_id=project_id,
        compute_enabled=flags.get("compute_enabled", True),
        dns_enabled=flags.get("dns_enabled", True),
        sqladmin_enabled=flags.get("sqladmin_enabled", True),
        container_enabled=flags.get("container_enabled", True),
    )

def _make_subscription_dict(sub_id="sub-001", display_name="Test Sub"):
    return {"id": sub_id, "display_name": display_name, "tenant_id": "tenant-1", "state": "Enabled"}
```

**New class: TestEnumerateAccountsGCP**
Tests for `_enumerate_accounts` GCP path (import `_enumerate_accounts` from scan module):

1. `test_enumerate_gcp_calls_with_six_args`: Mock `enumerate_gcp_projects` (return realistic `[ProjectInfo(...)]`), call `_enumerate_accounts(["gcp"])`, assert `enumerate_gcp_projects` was called with exactly 6 args: `(credentials, adc_project, None, None, None, None)`. Use `mock_call.call_args` to verify arg count and values.

2. `test_enumerate_gcp_extracts_project_id_from_project_info`: Mock `enumerate_gcp_projects` returning `[_make_project_info("my-proj"), _make_project_info("other-proj")]`, call `_enumerate_accounts(["gcp"])`, assert result `["gcp"]["accounts"]` contains `[{"id": "my-proj", ...}, {"id": "other-proj", ...}]` — verifying `.project_id` was extracted, not the raw object.

3. `test_enumerate_gcp_returns_error_on_exception`: Mock `enumerate_gcp_projects` to raise `Exception("Permission denied: projects.list")`, call `_enumerate_accounts(["gcp"])`, assert result `["gcp"]["error"]` contains "Permission denied" and `["gcp"]["accounts"]` is empty list.

**New class: TestEnumerateAccountsAzure**

4. `test_enumerate_azure_uses_id_key`: Mock `list_subscriptions` returning `[_make_subscription_dict("sub-001", "My Sub")]`, call `_enumerate_accounts(["azure"])`, assert result `["azure"]["accounts"][0]["id"]` == "sub-001".

5. `test_enumerate_azure_display_name_fallback_uses_id`: Mock `list_subscriptions` returning `[{"id": "sub-001", "display_name": "", "tenant_id": "t", "state": "Enabled"}]`, call `_enumerate_accounts(["azure"])`, assert `display_name` fallback works (display_name is "" but result still has the id).

6. `test_enumerate_azure_returns_error_on_exception`: Mock `list_subscriptions` to raise, verify error is captured.

**New class: TestBuildDiscoveryProvidersGCPCallSignature**

7. `test_build_gcp_calls_enumerate_with_six_args`: Mock `enumerate_gcp_projects` and call `_build_discovery_providers` with GCP config (include/exclude patterns set). Assert `enumerate_gcp_projects` called with `(credentials, adc_project, None, None, include, exclude)` — all 6 positional args.

8. `test_build_gcp_passes_include_exclude_patterns`: Create config with `include_accounts={"gcp": ["proj-*"]}` and `exclude_accounts={"gcp": ["proj-test"]}`. Verify the include/exclude lists are passed as args 5 and 6 to `enumerate_gcp_projects`.

**Mock patterns:**
- Use `sys.modules` injection for `google.auth` and `azure.identity` stubs (same pattern as existing tests in this file).
- Pre-import `cloud_usage.providers.gcp.projects` and `cloud_usage.providers.azure.subscriptions` before `mock.patch`.
- Mock at the function level: `mock.patch("cloud_usage.providers.gcp.projects.enumerate_gcp_projects")`.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_scan.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
  - _enumerate_accounts GCP tests verify 6-arg call and ProjectInfo.project_id extraction
  - _enumerate_accounts Azure tests verify "id" key access and display_name fallback
  - _build_discovery_providers GCP test verifies 6-arg call with include/exclude patterns
  - Error path tests verify error message capture for GCP and Azure
  - All new tests pass alongside existing checkpoint_engine tests
  </done>
</task>

<task type="auto">
  <name>Task 2: Add wizard error UX tests and CLI-vs-dashboard parity test</name>
  <files>tests/test_dashboard_wizard.py</files>
  <action>
Add new test classes to `tests/test_dashboard_wizard.py` covering the error UX and parity. Follow the existing test patterns: use the `client` fixture with `TestClient(app)`.

**New class: TestWizardErrorUX**

1. `test_wizard_accounts_shows_inline_error_for_failed_provider`: Mock `_enumerate_accounts` to return `{"aws": {"accounts": [{"id": "111", "display_name": "Acct 111"}], "error": None}, "gcp": {"accounts": [], "error": "Permission denied: resourcemanager.projects.list"}}`. POST to `/wizard/accounts` with providers=["aws", "gcp"]. Assert response contains "Permission denied" (inline error for GCP) AND "111" (AWS accounts still displayed).

2. `test_wizard_accounts_shows_blocking_when_all_fail`: Mock `_enumerate_accounts` to return all providers with empty accounts and error messages. POST to `/wizard/accounts` with providers=["aws", "gcp"]. Assert response contains "No providers available" or equivalent blocking message.

3. `test_wizard_accounts_working_provider_not_blocked_by_failed`: Mock `_enumerate_accounts` with AWS succeeding (2 accounts) and GCP failing. POST to `/wizard/accounts`. Assert the "Next: Review" button is NOT disabled (the `disabled` attribute is absent). Assert the AWS accounts are rendered with checkboxes.

4. `test_wizard_filter_accounts_uses_new_return_structure`: Mock `_enumerate_accounts` to return new structure. GET `/wizard/filter-accounts?provider=aws&q=111`. Assert response contains the filtered account.

**New class: TestCLIDashboardParity**

5. `test_dashboard_and_cli_gcp_enumeration_parity`: This test verifies that the dashboard's `_enumerate_accounts` produces the same project IDs as the CLI path would.
   - Create mock `ProjectInfo` list: `[ProjectInfo("proj-1", True, True, True, True), ProjectInfo("proj-2", True, False, True, False)]`.
   - Mock `enumerate_gcp_projects` to return this list.
   - Call `_enumerate_accounts(["gcp"])`.
   - Extract IDs: `[a["id"] for a in result["gcp"]["accounts"]]`.
   - Assert IDs match `["proj-1", "proj-2"]` — same project_id strings the CLI would see.
   - This proves the dashboard extracts `.project_id` correctly, matching CLI behavior.

**Mock patterns:**
- For wizard endpoint tests (1-4): use `@patch("cloud_usage.dashboard.routes.scan._enumerate_accounts")` to mock at the function level (same pattern as existing `TestWizardAccounts`).
- For parity test (5): use `sys.modules` injection + `mock.patch("cloud_usage.providers.gcp.projects.enumerate_gcp_projects")` to mock at the SDK level, letting `_enumerate_accounts` call the real code path (same pattern as `test_dashboard_scan.py`).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_wizard.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
  - Inline error rendering verified for single-provider failure
  - All-providers-fail blocking message verified
  - Working provider not blocked by failed provider verified
  - Filter accounts endpoint works with new return structure
  - CLI-vs-dashboard parity test proves identical project ID extraction
  - All new tests pass alongside existing wizard tests
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard_scan.py -x -v` — all tests pass (existing + new)
2. `python -m pytest tests/test_dashboard_wizard.py -x -v` — all tests pass (existing + new)
3. `python -m pytest tests/ -x --timeout=120` — full test suite passes with no regressions
</verification>

<success_criteria>
- At least 8 new regression tests covering all 4 bug fixes
- Error UX tests cover: single-provider failure (inline error), all-providers-fail (blocking message), working provider not blocked
- CLI-vs-dashboard parity test proves project ID extraction matches
- Azure display_name fallback test confirms correct behavior
- All existing tests continue to pass
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-dashboard-gcp-scan-fix/08-02-SUMMARY.md`
</output>
