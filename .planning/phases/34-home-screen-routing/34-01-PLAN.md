---
phase: 34-home-screen-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard_home_routing.py
  - tests/test_dashboard_app.py
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - ROUTE-01
  - ROUTE-02
  - DESIGN-02

must_haves:
  truths:
    - "test_dashboard_home_routing.py exists with xfail stubs for all home/routing behaviors"
    - "test_dashboard_app.py no longer asserts the old 'UDDI Estimator' title string for the root route"
    - "Running the test suite does not error on missing test functions referenced by later plans"
  artifacts:
    - path: "tests/test_dashboard_home_routing.py"
      provides: "xfail test stubs for HOME-01, HOME-02, ROUTE-01, ROUTE-02, DESIGN-02"
      exports:
        - test_home_returns_200_with_title
        - test_home_contains_three_calculator_cards
        - test_home_cards_contain_entry_links
        - test_cloud_route_returns_200
        - test_nios_route_returns_200
        - test_ad_route_returns_200
        - test_root_is_home_not_tab_dashboard
        - test_home_uses_calculator_card_class
  key_links:
    - from: "tests/test_dashboard_home_routing.py"
      to: "cloud_usage.dashboard.app.create_app"
      via: "TestClient(create_app())"
      pattern: "from cloud_usage\\.dashboard\\.app import create_app"
---

<objective>
Create the Wave 0 test scaffold for Phase 34: xfail stubs for all home-screen and routing behaviors, and update the existing title assertion in test_dashboard_app.py that will break when the home screen lands.

Purpose: Tests must exist (and fail predictably) before implementation. Later plans turn these xfails green. Without this Wave 0 plan, Plans 02 and 03 have no Nyquist-compliant verify commands.

Output: tests/test_dashboard_home_routing.py (8 xfail stubs), tests/test_dashboard_app.py updated (old title assertion updated to xfail with a reason comment).
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-home-screen-routing/34-RESEARCH.md
@.planning/phases/34-home-screen-routing/34-VALIDATION.md

<interfaces>
<!-- Confirmed from tests/test_dashboard_app.py and tests/test_dashboard_design.py -->

TestClient pattern used across all dashboard tests:
```python
from fastapi.testclient import TestClient
from cloud_usage.dashboard.app import create_app

app = create_app()

# Context-manager form used to ensure lifespan runs:
with TestClient(app) as client:
    response = client.get("/")
    assert response.status_code == 200
```

Existing assertion to update in tests/test_dashboard_app.py (line ~49):
```python
assert "UDDI Estimator" in response.text  # <-- must become xfail or updated title
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_dashboard_home_routing.py with xfail stubs</name>
  <files>tests/test_dashboard_home_routing.py</files>
  <behavior>
    - test_home_returns_200_with_title: GET / returns 200 and "Infoblox UDDI Token Calculator" in text — xfail (not yet implemented)
    - test_home_contains_three_calculator_cards: GET / HTML contains exactly three .calculator-card elements — xfail
    - test_home_cards_contain_entry_links: GET / HTML contains href="/cloud", href="/nios", href="/ad" — xfail
    - test_cloud_route_returns_200: GET /cloud returns 200 — xfail
    - test_nios_route_returns_200: GET /nios returns 200 — xfail
    - test_ad_route_returns_200: GET /ad returns 200 — xfail
    - test_root_is_home_not_tab_dashboard: GET / HTML does NOT contain 'nav.tab-bar' or 'hx-get="/tab/progress"' — xfail
    - test_home_uses_calculator_card_class: GET / HTML contains 'class="calculator-card"' — xfail
  </behavior>
  <action>
    Create tests/test_dashboard_home_routing.py. Use the existing TestClient/create_app pattern confirmed in the codebase.

    Each test must be decorated with @pytest.mark.xfail(strict=True, reason="Phase 34 not yet implemented") so the suite stays green (xfail = expected failure) and turns green automatically once the implementation lands (at which point strict=True makes it XPASS, which is passing).

    File structure:
    ```python
    import pytest
    from fastapi.testclient import TestClient
    from cloud_usage.dashboard.app import create_app

    app = create_app()


    @pytest.mark.xfail(strict=True, reason="Phase 34: home screen not yet implemented")
    def test_home_returns_200_with_title() -> None:
        with TestClient(app) as client:
            response = client.get("/")
        assert response.status_code == 200
        assert "Infoblox UDDI Token Calculator" in response.text


    @pytest.mark.xfail(strict=True, reason="Phase 34: home screen not yet implemented")
    def test_home_contains_three_calculator_cards() -> None:
        with TestClient(app) as client:
            response = client.get("/")
        assert response.status_code == 200
        assert response.text.count('class="calculator-card"') == 3


    @pytest.mark.xfail(strict=True, reason="Phase 34: home screen not yet implemented")
    def test_home_cards_contain_entry_links() -> None:
        with TestClient(app) as client:
            response = client.get("/")
        assert 'href="/cloud"' in response.text
        assert 'href="/nios"' in response.text
        assert 'href="/ad"' in response.text


    @pytest.mark.xfail(strict=True, reason="Phase 34: /cloud route not yet implemented")
    def test_cloud_route_returns_200() -> None:
        with TestClient(app) as client:
            response = client.get("/cloud")
        assert response.status_code == 200


    @pytest.mark.xfail(strict=True, reason="Phase 34: /nios route not yet implemented")
    def test_nios_route_returns_200() -> None:
        with TestClient(app) as client:
            response = client.get("/nios")
        assert response.status_code == 200


    @pytest.mark.xfail(strict=True, reason="Phase 34: /ad route not yet implemented")
    def test_ad_route_returns_200() -> None:
        with TestClient(app) as client:
            response = client.get("/ad")
        assert response.status_code == 200


    @pytest.mark.xfail(strict=True, reason="Phase 34: home screen not yet implemented")
    def test_root_is_home_not_tab_dashboard() -> None:
        with TestClient(app) as client:
            response = client.get("/")
        assert 'hx-get="/tab/progress"' not in response.text


    @pytest.mark.xfail(strict=True, reason="Phase 34: home screen not yet implemented")
    def test_home_uses_calculator_card_class() -> None:
        with TestClient(app) as client:
            response = client.get("/")
        assert 'class="calculator-card"' in response.text
    ```

    Do NOT use strict=False — we want XPASS to surface once implementation is done.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_home_routing.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 8 tests exist and report xfail (x) — not error, not skip, not fail. Full suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Update test_dashboard_app.py old title assertion</name>
  <files>tests/test_dashboard_app.py</files>
  <action>
    Find the test in TestAppFactory that asserts `"UDDI Estimator" in response.text` for the GET / route (test_index_returns_200_with_title, around line 41-49).

    After Phase 34, the root route renders home.html which has title "Infoblox UDDI Token Calculator", not "UDDI Estimator". Mark this specific test xfail so the suite stays green now but will pass once Plan 02 lands.

    Change:
    ```python
    def test_index_returns_200_with_title(self) -> None:
        ...
        assert "UDDI Estimator" in response.text
    ```

    To:
    ```python
    @pytest.mark.xfail(strict=True, reason="Phase 34: root renders home.html with new title")
    def test_index_returns_200_with_title(self) -> None:
        ...
        assert "UDDI Estimator" in response.text
    ```

    Also ensure `import pytest` is present at the top of the file if not already there.

    Do NOT change any other tests in this file. Do NOT change the app.title == "UDDI Estimator" assertion (that's the FastAPI app title metadata, not the HTML page title — it stays correct).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_app.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>test_index_returns_200_with_title reports xfail (x). All other tests in test_dashboard_app.py remain green. Full suite still passes.</done>
</task>

</tasks>

<verification>
Full suite green after wave 1:
```
cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/ -x -q 2>&1 | tail -10
```
Expected: all pass + xfail markers shown for the 9 stubs (8 new + 1 updated). Zero errors, zero unexpected failures.
</verification>

<success_criteria>
- tests/test_dashboard_home_routing.py exists with 8 xfail stubs (HOME-01 x2, HOME-02, ROUTE-01 x3, ROUTE-02, DESIGN-02)
- tests/test_dashboard_app.py test_index_returns_200_with_title marked xfail
- Full test suite runs green with xfail markers
</success_criteria>

<output>
After completion, create `.planning/phases/34-home-screen-routing/34-01-SUMMARY.md`
</output>
