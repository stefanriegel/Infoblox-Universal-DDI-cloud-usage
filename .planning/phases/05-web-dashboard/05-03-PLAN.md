---
phase: 05-web-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/cloud_usage/dashboard/routes/partials.py
  - src/cloud_usage/dashboard/templates/pages/results.html
  - src/cloud_usage/dashboard/templates/pages/summary.html
  - src/cloud_usage/dashboard/templates/partials/results_table.html
  - src/cloud_usage/dashboard/templates/partials/filter_chips.html
  - src/cloud_usage/dashboard/templates/partials/summary_cards.html
  - src/cloud_usage/dashboard/templates/components/pagination.html
  - src/cloud_usage/dashboard/app.py
  - tests/test_dashboard_results.py
autonomous: true
requirements:
  - PLAT-03

must_haves:
  truths:
    - "User can view a sortable data table of discovered resources with columns matching XLS output"
    - "User can filter results by provider, account, resource type, category, and counted/skipped status via dropdown filters"
    - "Active filters are shown as removable chips above the results table"
    - "Summary cards show Total Tokens, DDI count, IP count, Asset count, and per-provider breakdown"
    - "Results table uses server-side pagination with 50 rows per page"
  artifacts:
    - path: "src/cloud_usage/dashboard/routes/partials.py"
      provides: "HTMX partial fragment routes for filtering, pagination, and sorting"
      exports: ["router"]
    - path: "src/cloud_usage/dashboard/templates/pages/results.html"
      provides: "Results tab with filter bar, table, and pagination"
      contains: "filter_provider"
    - path: "src/cloud_usage/dashboard/templates/pages/summary.html"
      provides: "Summary tab with token summary cards and per-provider breakdown"
      contains: "summary-cards"
    - path: "src/cloud_usage/dashboard/templates/partials/results_table.html"
      provides: "Paginated results table body for HTMX swap"
      contains: "results-body"
    - path: "src/cloud_usage/dashboard/templates/partials/summary_cards.html"
      provides: "Token summary cards partial"
      contains: "Total Tokens"
  key_links:
    - from: "src/cloud_usage/dashboard/routes/partials.py"
      to: "src/cloud_usage/dashboard/services/scan_manager.py"
      via: "request.app.state.scan_manager.resources for filtering"
      pattern: "scan_manager\\.resources"
    - from: "src/cloud_usage/dashboard/templates/pages/results.html"
      to: "src/cloud_usage/dashboard/routes/partials.py"
      via: "hx-get=/partials/results-table for filter-triggered swap"
      pattern: "hx-get.*partials/results-table"
    - from: "src/cloud_usage/dashboard/routes/partials.py"
      to: "src/cloud_usage/counting/token_calculator.py"
      via: "calculate_account_tokens/calculate_provider_tokens for summary"
      pattern: "calculate_.*_tokens"
---

<objective>
Implement the Results tab with filterable data table and the Summary tab with token calculation cards.

Purpose: Delivers PLAT-03 -- users can browse results with filtering by provider, account, resource type, category, and status, and see token calculation summaries.
Output: Working Results and Summary tabs with server-side filtering, pagination, removable filter chips, and token summary cards.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-web-dashboard/05-RESEARCH.md
@.planning/phases/05-web-dashboard/05-01-SUMMARY.md
@src/cloud_usage/schema/resource.py
@src/cloud_usage/counting/token_calculator.py
@src/cloud_usage/counting/categorizer.py
@src/cloud_usage/dashboard/app.py
@src/cloud_usage/dashboard/services/scan_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Results table with server-side filtering, pagination, and filter chips</name>
  <files>
    src/cloud_usage/dashboard/routes/partials.py
    src/cloud_usage/dashboard/routes/pages.py
    src/cloud_usage/dashboard/templates/pages/results.html
    src/cloud_usage/dashboard/templates/partials/results_table.html
    src/cloud_usage/dashboard/templates/partials/filter_chips.html
    src/cloud_usage/dashboard/templates/components/pagination.html
    src/cloud_usage/dashboard/app.py
  </files>
  <action>
    Implement the Results tab with filtering, sorting, and pagination:

    1. **routes/partials.py** -- Create partials router with filtering logic:
       - `GET /partials/results-table` -- accepts query params: `filter_provider`, `filter_account`, `filter_resource_type`, `filter_category`, `filter_status`, `sort_by`, `sort_dir`, `page` (default 1), `page_size` (default 50)
       - Get resources from `request.app.state.scan_manager.resources`
       - Apply filters sequentially: each filter is optional, applied only if non-empty
         - `filter_provider`: match `resource.provider` (case-insensitive)
         - `filter_account`: match `resource.account_id`
         - `filter_resource_type`: match `resource.resource_type`
         - `filter_category`: match `resource.category` (ddi/ip/asset)
         - `filter_status`: "counted" -> `resource.counted is True`, "skipped" -> `resource.counted is False`
       - Apply sorting: sort by the `sort_by` field (resource_id, resource_type, provider, account_id, region, category) in `sort_dir` (asc/desc). Default sort by resource_type asc.
       - Apply pagination: calculate `total_pages`, slice results to current page
       - Build `active_filters` dict from non-empty filter params for chip display
       - Build `filter_options` dict with unique values from ALL resources (not filtered) for dropdown population
       - Render `partials/results_table.html` template with: `resources` (current page), `page`, `total_pages`, `total_results`, `active_filters`, `filter_options`, `sort_by`, `sort_dir`
       - Return HTMLResponse (or use templates.TemplateResponse for partials)

       - `GET /partials/filter-chips` -- render just the filter chips for HTMX swap
       - `GET /partials/empty` -- return empty string (for auto-dismiss banners)

    2. **templates/pages/results.html** -- Results tab full page content:
       - Tab bar include with `active_tab="results"` and resource count badge
       - Summary cards row above table (always visible per locked decision) -- include `partials/summary_cards.html`
       - Filter bar per Research Pattern 4 and locked decision:
         - Horizontal row of `<select>` dropdowns: Provider, Account, Resource Type, Category, Status
         - Each dropdown uses `hx-get="/partials/results-table" hx-target="#results-body" hx-trigger="change" hx-include="[name^='filter_']"`
         - Populate options from `filter_options` dict
       - Active filter chips area: include `partials/filter_chips.html`
       - Results data table per locked decision:
         - Columns: Resource ID, Type, Provider, Account, Region, Category, Counted, IPs, Skip Reason
         - Matching XLS output columns per locked decision
         - Sortable column headers: `<th><a hx-get="/partials/results-table?sort_by=resource_type&sort_dir=asc" hx-target="#results-body">Type</a></th>`
         - Table body in `<tbody id="results-body">` for HTMX swap target
       - Pagination controls below table: include `components/pagination.html`
       - Show "No results yet. Run a scan from the Progress tab." when no resources exist

    3. **templates/partials/results_table.html** -- Paginated table body partial:
       - Renders `<tr>` for each resource in the current page slice
       - Each row: resource_id, resource_type, provider, account_id, region, category (uppercase), counted (Yes/No), ip_addresses joined by comma, skip_reason
       - Include pagination partial at bottom
       - Include filter chips partial at top (so chips update with table)
       - Show result count: "Showing N-M of T results"

    4. **templates/partials/filter_chips.html** -- Active filter chip bar per locked decision:
       - For each active filter (non-empty), render a chip: `<span class="chip">{key}: {value} <a hx-get="/partials/results-table?clear_{key}=1" hx-target="#results-body">&times;</a></span>`
       - Clearing a filter re-fetches the table without that filter param

    5. **templates/components/pagination.html** -- Page controls:
       - Previous/Next buttons with `hx-get="/partials/results-table?page=N" hx-target="#results-body"`
       - Page number display: "Page X of Y"
       - Disabled Previous on page 1, disabled Next on last page
       - Use Pico CSS button group styling

    6. **routes/pages.py** -- Update `/tab/results` endpoint:
       - Render `pages/results.html` with scan_manager resources, summary data, and initial filter_options
       - Pass `active_tab="results"`, `resource_count=len(scan_manager.resources)`

    7. **app.py** -- Register partials router: `app.include_router(partials_router)` from routes/partials.py.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -c "from cloud_usage.dashboard.app import create_app; app = create_app(); routes = [r.path for r in app.routes]; assert any('partials' in str(r.path) for r in app.routes), f'Partials route missing: {routes}'; print('Partials route registered')"</automated>
    <manual>Start uvicorn, navigate to Results tab, verify filter dropdowns and table render</manual>
  </verify>
  <done>
    Results tab renders sortable data table with columns matching XLS output.
    Filter bar has Provider/Account/Resource Type/Category/Status dropdowns with instant HTMX filtering.
    Active filters shown as removable chips.
    Server-side pagination with 50 rows per page and Previous/Next controls.
    Empty state message shown when no scan results exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Summary tab with token cards and results filtering tests</name>
  <files>
    src/cloud_usage/dashboard/templates/pages/summary.html
    src/cloud_usage/dashboard/templates/partials/summary_cards.html
    src/cloud_usage/dashboard/routes/pages.py
    src/cloud_usage/dashboard/routes/partials.py
    tests/test_dashboard_results.py
  </files>
  <action>
    1. **templates/partials/summary_cards.html** -- Token summary cards per locked decision:
       - Summary cards grid (always visible on Results and Summary tabs):
         - Total Tokens: big number with label
         - DDI Objects: count
         - Active IPs: count (deduplicated)
         - Managed Assets: count
       - Per-provider breakdown section: for each provider, show provider name and token subtotal
       - Use Pico CSS `<article>` elements for card styling within the `.summary-cards` grid
       - Accept template variables: `total_tokens`, `ddi_count`, `ip_count`, `asset_count`, `provider_breakdown` (dict of provider -> token count)
       - Show "No scan data available" when all counts are zero

    2. **templates/pages/summary.html** -- Summary tab full page:
       - Tab bar with `active_tab="summary"`
       - Include `partials/summary_cards.html` with full token data
       - Detailed per-provider table: Provider, Accounts Scanned, DDI Objects, Active IPs, Managed Assets, Tokens
       - Per-account breakdown table: Account ID, Provider, DDI, IPs, Assets, Tokens (sortable)
       - Download section (placeholder for Plan 04): "Download reports" area with disabled buttons

    3. **routes/pages.py** -- Update `/tab/summary` endpoint:
       - Calculate summary data from scan_manager resources using counting pipeline functions:
         - Import `calculate_account_tokens`, `calculate_provider_tokens` from counting module
         - Import `deduplicate_ips_per_vpc` from ip_counter
         - Compute DDI/IP/Asset counts from resources
         - Build per-account and per-provider breakdowns
       - Pass all summary data to template
       - Handle empty state (no resources): pass zero counts

    4. **routes/partials.py** -- Add summary-related partial:
       - `GET /partials/summary-cards` -- render just summary cards for HTMX swap (used by Progress tab completion to update)

    5. **tests/test_dashboard_results.py** -- Write comprehensive tests:
       - **Test results table with no data**: GET `/tab/results` returns 200 with "No results" message
       - **Test results table with mock data**: Populate ScanManager with test CloudResource objects, GET `/tab/results` returns 200 with table rows
       - **Test filtering by provider**: GET `/partials/results-table?filter_provider=aws` returns only AWS resources
       - **Test filtering by category**: GET `/partials/results-table?filter_category=ddi` returns only DDI resources
       - **Test filtering by status**: GET `/partials/results-table?filter_status=counted` returns only counted resources
       - **Test pagination**: Create 120 mock resources, request page 1 (50 results), page 2 (50 results), page 3 (20 results)
       - **Test summary tab with no data**: GET `/tab/summary` returns 200 with zero counts
       - **Test summary tab with mock data**: Populate ScanManager, verify token totals match calculation
       - **Test filter chips render**: Verify active filters appear as chips in response HTML
       - Create helper `_make_test_resource(...)` factory for CloudResource test data
       - Use FastAPI TestClient, follow project test patterns
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && python -m pytest tests/test_dashboard_results.py -x -v 2>&1 | tail -30</automated>
    <manual>Verify summary cards show correct token calculations and per-provider breakdown</manual>
  </verify>
  <done>
    Summary tab renders token summary cards with Total Tokens, DDI, IP, Asset counts.
    Per-provider breakdown table shows provider-level token details.
    Per-account breakdown table shows account-level details.
    All filtering tests pass (provider, category, status, pagination).
    Summary calculations use existing counting pipeline functions for consistency with CLI output.
    All tests pass alongside existing 853+ test suite.
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard_results.py -x` passes all tests
2. Results tab shows sortable table with filtering by all five dimensions
3. Filter chips appear and are removable
4. Pagination works with Previous/Next controls
5. Summary tab shows correct token calculation matching CLI output
6. Empty states handled gracefully for both tabs
</verification>

<success_criteria>
- Results table matches XLS output columns per locked decision
- Five filter dropdowns (Provider, Account, Resource Type, Category, Status) apply instantly via HTMX
- Removable filter chips show active filters
- Server-side pagination with 50 rows per page
- Summary cards show Total Tokens, DDI count, IP count, Asset count
- Per-provider and per-account breakdown tables render correctly
- All tests pass (filtering, pagination, summary calculations)
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-dashboard/05-03-SUMMARY.md`
</output>
