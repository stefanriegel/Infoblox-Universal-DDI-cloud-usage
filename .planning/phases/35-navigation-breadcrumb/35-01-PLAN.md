---
phase: 35-navigation-breadcrumb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_dashboard_navigation.py
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "test_dashboard_navigation.py exists and all 7 stubs are xfail(strict=True)"
    - "Running pytest tests/test_dashboard_navigation.py shows 7 xfailed tests, 0 errors"
  artifacts:
    - path: "tests/test_dashboard_navigation.py"
      provides: "xfail stubs covering NAV-01 and NAV-02 breadcrumb behaviors"
      exports: []
  key_links:
    - from: "tests/test_dashboard_navigation.py"
      to: "cloud_usage.dashboard.app.create_app"
      via: "import and TestClient usage"
      pattern: "from cloud_usage.dashboard.app import create_app"
---

<objective>
Create the xfail test scaffold for Phase 35 breadcrumb navigation. All 7 stubs must fail before implementation lands (Wave 2), then turn green when `calculator_name` is injected and the breadcrumb block is added to `base.html`.

Purpose: Nyquist compliance — tests define the contract before implementation, preventing silent regressions.
Output: tests/test_dashboard_navigation.py with 7 xfail(strict=True) stubs covering NAV-01 and NAV-02.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-navigation-breadcrumb/35-RESEARCH.md

<interfaces>
<!-- Import pattern confirmed from tests/test_dashboard_home_routing.py -->
from __future__ import annotations
from fastapi.testclient import TestClient
from cloud_usage.dashboard.app import create_app

app = create_app()
# Each test creates its own `with TestClient(app) as client:` context
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test_dashboard_navigation.py with 7 xfail stubs</name>
  <files>tests/test_dashboard_navigation.py</files>
  <action>
Create `tests/test_dashboard_navigation.py` with 7 test stubs, all marked `@pytest.mark.xfail(strict=True, reason="Phase 35 Plan 02 not yet implemented")`.

Module docstring must list: requirements covered (NAV-01, NAV-02), what each test checks.

The 7 stubs:

1. `test_cloud_page_has_breadcrumb_home_link` — GET /cloud, assert `'href="/"'` in response.text AND "Home" in response.text (NAV-02)
2. `test_nios_page_has_breadcrumb_home_link` — GET /nios, same assertions (NAV-02)
3. `test_ad_page_has_breadcrumb_home_link` — GET /ad, same assertions (NAV-02)
4. `test_cloud_page_breadcrumb_shows_calculator_name` — GET /cloud, assert "Cloud Calculator" in response.text (NAV-01)
5. `test_nios_page_has_breadcrumb` — GET /nios, assert "NIOS Calculator" in response.text (NAV-01)
6. `test_ad_page_has_breadcrumb` — GET /ad, assert "AD Calculator" in response.text (NAV-01)
7. `test_home_has_no_breadcrumb` — GET /, assert "breadcrumb-nav" NOT in response.text (NAV-01 — breadcrumb must NOT appear on home screen)

Each test body:
```python
@pytest.mark.xfail(strict=True, reason="Phase 35 Plan 02 not yet implemented")
def test_cloud_page_has_breadcrumb_home_link() -> None:
    with TestClient(app) as client:
        response = client.get("/cloud")
    assert response.status_code == 200
    assert 'href="/"' in response.text
    assert "Home" in response.text
```

For `test_home_has_no_breadcrumb`, the assertion is `assert "breadcrumb-nav" not in response.text`.

IMPORTANT: `strict=True` means if the test PASSES it becomes a pytest error (XPASS = fail). This is correct — Plan 02 implementation will remove the xfail markers, not make them strict=False.

Do NOT use `strict=False`. Do NOT mix in non-xfail tests. All 7 stubs must be xfail(strict=True).
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_navigation.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>pytest reports 7 xfailed, 0 passed, 0 errors. All stubs present with correct assertion bodies.</done>
</task>

</tasks>

<verification>
Run: `source venv/bin/activate && python -m pytest tests/test_dashboard_navigation.py -v`
Expected: 7 xfailed (XFAIL), 0 errors
Full suite still green: `python -m pytest tests/ -x -q`
</verification>

<success_criteria>
- tests/test_dashboard_navigation.py exists with 7 xfail(strict=True) stubs
- pytest tests/test_dashboard_navigation.py shows 7 xfailed, 0 errors
- Full suite passes (no regressions from adding the test file)
- Tests cover NAV-01 (breadcrumb text on /cloud, /nios, /ad; absent on /) and NAV-02 (href="/" + "Home" text)
</success_criteria>

<output>
After completion, create `.planning/phases/35-navigation-breadcrumb/35-01-SUMMARY.md`
</output>
