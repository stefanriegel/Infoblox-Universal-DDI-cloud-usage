---
phase: 35-navigation-breadcrumb
plan: 02
type: execute
wave: 2
depends_on:
  - 35-01
files_modified:
  - src/cloud_usage/dashboard/routes/pages.py
  - src/cloud_usage/dashboard/templates/base.html
  - src/cloud_usage/dashboard/static/app.css
autonomous: true
requirements:
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "GET /cloud response contains breadcrumb-nav with 'Cloud Calculator' text and href='/'"
    - "GET /nios response contains breadcrumb-nav with 'NIOS Calculator' text and href='/'"
    - "GET /ad response contains breadcrumb-nav with 'AD Calculator' text and href='/'"
    - "GET / (home) response does NOT contain 'breadcrumb-nav'"
    - "Breadcrumb persists through HTMX tab swaps because it lives outside #tab-container"
    - "All 7 tests in test_dashboard_navigation.py pass (xfail markers removed)"
  artifacts:
    - path: "src/cloud_usage/dashboard/routes/pages.py"
      provides: "calculator_name context injection in /cloud, /nios, /ad route handlers"
    - path: "src/cloud_usage/dashboard/templates/base.html"
      provides: "conditional breadcrumb nav block between </header> and <main>"
    - path: "src/cloud_usage/dashboard/static/app.css"
      provides: "breadcrumb-nav, breadcrumb, breadcrumb-item, breadcrumb-current CSS rules"
  key_links:
    - from: "src/cloud_usage/dashboard/routes/pages.py"
      to: "src/cloud_usage/dashboard/templates/base.html"
      via: "calculator_name context variable in TemplateResponse call"
      pattern: "calculator_name.*Calculator"
    - from: "src/cloud_usage/dashboard/templates/base.html"
      to: "src/cloud_usage/dashboard/static/app.css"
      via: "class='breadcrumb-nav' referencing CSS rules"
      pattern: "breadcrumb-nav"
---

<objective>
Implement the breadcrumb navigation: inject `calculator_name` into the three calculator route handlers, add the conditional `<nav class="breadcrumb-nav">` block to `base.html` between `</header>` and `<main>`, and add ~20 lines of breadcrumb CSS to `app.css`. Remove xfail markers from the 7 stubs so all tests turn green.

Purpose: Satisfies NAV-01 (breadcrumb displays "Home > [Calculator Name]" on all calculator pages) and NAV-02 (clicking "Home" in breadcrumb navigates to /).
Output: Modified pages.py, base.html, app.css; updated test file with xfail markers removed.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-navigation-breadcrumb/35-RESEARCH.md
@.planning/phases/35-navigation-breadcrumb/35-01-SUMMARY.md

<interfaces>
<!-- From 35-RESEARCH.md — confirmed by codebase inspection -->

<!-- pages.py route handler pattern (lines 301-322) -->
@router.get("/cloud", response_class=HTMLResponse)
async def cloud_calculator(request: Request) -> HTMLResponse:
    templates = request.app.state.templates
    context = _get_tab_context(request, "progress")
    # ADD: context["calculator_name"] = "Cloud Calculator"
    return templates.TemplateResponse(request, "base.html", context)

@router.get("/nios", response_class=HTMLResponse)
async def nios_calculator(request: Request) -> HTMLResponse:
    templates = request.app.state.templates
    context = _get_tab_context(request, "nios")
    # ADD: context["calculator_name"] = "NIOS Calculator"
    return templates.TemplateResponse(request, "base.html", context)

@router.get("/ad", response_class=HTMLResponse)
async def ad_calculator(request: Request) -> HTMLResponse:
    templates = request.app.state.templates
    context = _get_tab_context(request, "ad")
    # ADD: context["calculator_name"] = "AD Calculator"
    return templates.TemplateResponse(request, "base.html", context)

<!-- base.html structure (lines 11-18) -->
<header class="site-header">   ← line 11
    ...
</header>                       ← line 17
<!-- INSERT breadcrumb nav HERE -->
<main class="container" id="main-content">  ← line 18

<!-- Design tokens already in design-system.css (confirmed) -->
--ib-gray-100, --ib-gray-200, --ib-gray-600, --ib-blue, --ib-blue-dark
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject calculator_name into /cloud, /nios, /ad route handlers</name>
  <files>src/cloud_usage/dashboard/routes/pages.py</files>
  <behavior>
    - GET /cloud context contains calculator_name = "Cloud Calculator"
    - GET /nios context contains calculator_name = "NIOS Calculator"
    - GET /ad context contains calculator_name = "AD Calculator"
    - GET / (index) context does NOT contain calculator_name (home.html renders separately)
    - _get_tab_context() is NOT modified — injection happens after calling it
  </behavior>
  <action>
Read pages.py. Find the three route handlers: `cloud_calculator`, `nios_calculator`, `ad_calculator`. In each handler, add `context["calculator_name"] = "[Name] Calculator"` on the line immediately after `context = _get_tab_context(request, ...)` and before the `return` statement.

Exact values:
- /cloud handler: `context["calculator_name"] = "Cloud Calculator"`
- /nios handler: `context["calculator_name"] = "NIOS Calculator"`
- /ad handler: `context["calculator_name"] = "AD Calculator"`

Do NOT modify `_get_tab_context()`. Do NOT modify the `index()` handler that serves `/`. Do NOT modify any `/tab/*` handlers.

After editing, verify the three route handlers each have the calculator_name line. The `index()` route must NOT have it.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_navigation.py::test_cloud_page_breadcrumb_shows_calculator_name tests/test_dashboard_navigation.py::test_nios_page_has_breadcrumb tests/test_dashboard_navigation.py::test_ad_page_has_breadcrumb -x -q 2>&1 | tail -8</automated>
  </verify>
  <done>The three calculator name tests pass (xfail markers removed first). The home test and breadcrumb-link tests still fail/xfail until base.html is updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add breadcrumb nav to base.html and CSS to app.css; remove xfail markers</name>
  <files>
    src/cloud_usage/dashboard/templates/base.html
    src/cloud_usage/dashboard/static/app.css
    tests/test_dashboard_navigation.py
  </files>
  <action>
**base.html:** Read the file. Insert the breadcrumb nav block between `</header>` and `<main class="container" ...>`. Use the exact Jinja2 snippet:

```html
    {% if calculator_name %}
    <nav class="breadcrumb-nav" aria-label="breadcrumb">
        <div class="container">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">Home</a></li>
                <li class="breadcrumb-item breadcrumb-current" aria-current="page">{{ calculator_name }}</li>
            </ol>
        </div>
    </nav>
    {% endif %}
```

The `<a href="/">Home</a>` must have NO `hx-*` attributes — plain anchor only. Full page navigation back to home is correct; HTMX must NOT be used on this link.

**app.css:** Read the file. Append the following breadcrumb CSS block at the end (after existing rules):

```css
/* ===== Breadcrumb navigation ===== */
.breadcrumb-nav {
    background: var(--ib-gray-100);
    border-bottom: 1px solid var(--ib-gray-200);
    padding: 0.5rem 0;
}

.breadcrumb {
    display: flex;
    list-style: none;
    padding: 0;
    margin: 0;
    gap: 0;
    align-items: center;
    font-size: 0.875rem;
}

.breadcrumb-item + .breadcrumb-item::before {
    content: ">";
    padding: 0 0.5rem;
    color: var(--ib-gray-600);
}

.breadcrumb-item a {
    color: var(--ib-blue);
    text-decoration: none;
}

.breadcrumb-item a:hover {
    text-decoration: underline;
    color: var(--ib-blue-dark);
}

.breadcrumb-current {
    color: var(--ib-gray-600);
    font-weight: 500;
}
```

**tests/test_dashboard_navigation.py:** Read the file. Remove ALL `@pytest.mark.xfail(...)` decorators from all 7 tests. The test bodies (assertions) remain unchanged. The module docstring can be updated to remove the xfail note if present. Do NOT modify any test logic.

Do NOT add the breadcrumb block anywhere inside `<main>` or inside any partial template. Do NOT add `hx-*` attributes to the Home link. Do NOT modify `home.html`.
  </action>
  <verify>
    <automated>cd /Users/mustermann/Documents/coding/Infoblox-Universal-DDI-cloud-usage && source venv/bin/activate && python -m pytest tests/test_dashboard_navigation.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>All 7 tests in test_dashboard_navigation.py PASS (no xfail, no skip). Full suite green: python -m pytest tests/ -x -q passes.</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_dashboard_navigation.py -v` — 7 passed, 0 failed, 0 xfailed
2. `python -m pytest tests/ -x -q` — full suite green (no regressions)
3. Confirm breadcrumb is outside `#tab-container` in base.html (grep: breadcrumb-nav appears before `<main`)
4. Confirm `home.html` is unchanged (no breadcrumb reference added)
5. Confirm no `hx-*` attributes on the Home breadcrumb link
</verification>

<success_criteria>
- All 7 tests in test_dashboard_navigation.py pass without xfail markers
- GET /cloud contains "breadcrumb-nav", "Cloud Calculator", href="/"
- GET /nios contains "breadcrumb-nav", "NIOS Calculator", href="/"
- GET /ad contains "breadcrumb-nav", "AD Calculator", href="/"
- GET / does NOT contain "breadcrumb-nav"
- Full pytest suite passes with no regressions
- Breadcrumb uses plain `<a href="/">Home</a>` with no HTMX attributes
- CSS uses only existing --ib-* design tokens (no new tokens added)
</success_criteria>

<output>
After completion, create `.planning/phases/35-navigation-breadcrumb/35-02-SUMMARY.md`
</output>
