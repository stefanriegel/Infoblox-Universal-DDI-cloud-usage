---
phase: 20-migration-wizard-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/cloud_usage/dashboard/templates/partials/nios/step2_run.html
  - src/cloud_usage/dashboard/templates/pages/nios.html
autonomous: false
requirements:
  - WIZ-04

must_haves:
  truths:
    - "When NIOS analysis starts running, the user sees 'Step 3: Run Analysis' as the section header"
    - "The full wizard step sequence is visually consistent: Step 1 Upload Backup, Step 2 Assign Members, Step 3 Run Analysis"
  artifacts:
    - path: "src/cloud_usage/dashboard/templates/partials/nios/step2_run.html"
      provides: "Running state article header 'Step 3: Run Analysis' (HTMX partial swap path)"
      contains: "Step 3: Run Analysis"
    - path: "src/cloud_usage/dashboard/templates/pages/nios.html"
      provides: "Running state article header 'Step 3: Run Analysis' (full tab inline path)"
      contains: "Step 3: Run Analysis"
  key_links:
    - from: "POST /nios/run"
      to: "step2_run.html"
      via: "HTMX hx-target=#nios-wizard hx-swap=innerHTML"
      pattern: "hx-post.*nios/run"
    - from: "nios.html running block"
      to: "nios.html inline article"
      via: "Jinja2 conditional nios_state == 'running'"
      pattern: "nios_state.*running"
---

<objective>
Complete WIZ-04 step label consistency by updating the running-state article header in both rendering paths — the HTMX partial (`step2_run.html`) and the full-tab inline block (`nios.html`) — to read "Step 3: Run Analysis".

Purpose: The HTMX swap and the full-tab Jinja2 conditional are separate rendering paths, both needing the same header update for a consistent user experience.
Output: `step2_run.html` and `nios.html` both display "Step 3: Run Analysis" when analysis is running.
</objective>

<execution_context>
@/Users/mustermann/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mustermann/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-migration-wizard-ux/20-CONTEXT.md

<interfaces>
<!-- Current structure of step2_run.html -->
```jinja2
{# NIOS Wizard: Running state partial.
   Swapped into #nios-wizard when POST /nios/run is called.
   SSE-connected: nios_progress events trigger hx-get for rendered progress;
   nios_complete triggers full tab refresh.
   Variables: nios_state (str) #}
<article>
  <header>NIOS Analysis Running</header>   {# <-- update this #}
  <div hx-ext="sse" sse-connect="/api/sse/nios">
    ...
  </div>
</article>
```

<!-- nios.html running block (Jinja2 conditional within the full tab) -->
The nios.html page has a block that renders when `nios_state == 'running'` (or similar),
containing an `<article>` with `<header>NIOS Analysis Running</header>`.
Update that `<header>` to `<header>Step 3: Run Analysis</header>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update running-state headers to "Step 3: Run Analysis"</name>
  <files>
    src/cloud_usage/dashboard/templates/partials/nios/step2_run.html,
    src/cloud_usage/dashboard/templates/pages/nios.html
  </files>
  <action>
    1. In `step2_run.html`: Replace the `<header>` content inside the top-level `<article>`:
       - Old: `<header>NIOS Analysis Running</header>`
       - New: `<header>Step 3: Run Analysis</header>`

    2. In `nios.html`: Find the running state block (inline article rendered when analysis is running).
       Replace its `<header>` content:
       - Old: `<header>NIOS Analysis Running</header>`
       - New: `<header>Step 3: Run Analysis</header>`

    These are two independent rendering paths for the same logical state — both must be updated.
    Do not change any other content in either file.
  </action>
  <verify>
    grep -n "Step 3: Run Analysis" src/cloud_usage/dashboard/templates/partials/nios/step2_run.html
    grep -n "Step 3: Run Analysis" src/cloud_usage/dashboard/templates/pages/nios.html
    grep "NIOS Analysis Running" src/cloud_usage/dashboard/templates/partials/nios/step2_run.html src/cloud_usage/dashboard/templates/pages/nios.html
  </verify>
  <done>
    Both `step2_run.html` and `nios.html` contain `<header>Step 3: Run Analysis</header>`. Neither file contains the old text "NIOS Analysis Running".
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full wizard UX end-to-end</name>
  <action>Human verifies all four WIZ requirements work correctly in the running application.</action>
  <what-built>
    Full wizard step label sequence across all three states:
    - Step 1: Upload NIOS Grid Backup (step1_upload.html, already present)
    - Step 2: Assign Members (step1_upload.html, updated by Plan 01)
    - Step 3: Run Analysis (step2_run.html + nios.html, updated by this plan)
    Plus: explanatory article, Select All/Clear All, live counter from Plan 01.
  </what-built>
  <how-to-verify>
    1. Start the app: `uvicorn cloud_usage.main:app --reload` from the `src/` directory
    2. Open the NIOS tab in the browser
    3. Confirm: "Step 1: Upload NIOS Grid Backup" heading is visible
    4. Upload a `.tar.gz` NIOS backup file
    5. Confirm after upload:
       - Explanatory article about NIOS/NIOSX formulas is visible above the form
       - "Step 2: Assign Members" heading appears above the member table
       - "Select All" and "Clear All" buttons are present
       - Live counter shows "NIOS: N members | NIOSX: 0 members" initially
    6. Click "Select All" — confirm counter updates to "NIOS: 0 members | NIOSX: N members"
    7. Click "Clear All" — confirm counter resets to "NIOS: N members | NIOSX: 0 members"
    8. Toggle individual checkboxes — confirm counter updates immediately on each change
    9. Click "Run Analysis"
    10. Confirm: article header reads "Step 3: Run Analysis" (not "NIOS Analysis Running")
  </how-to-verify>
  <verify>Human confirmation</verify>
  <done>All four WIZ requirements verified working in the running application. Checkpoint approved.</done>
  <resume-signal>Type "approved" to complete the phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After Task 1:

1. `grep -c "Step 3: Run Analysis" src/cloud_usage/dashboard/templates/partials/nios/step2_run.html` returns 1
2. `grep -c "Step 3: Run Analysis" src/cloud_usage/dashboard/templates/pages/nios.html` returns 1
3. `grep "NIOS Analysis Running" src/cloud_usage/dashboard/templates/partials/nios/step2_run.html src/cloud_usage/dashboard/templates/pages/nios.html` returns empty

Full wizard sequence confirmed by checkpoint.
</verification>

<success_criteria>
- `step2_run.html` `<header>` reads "Step 3: Run Analysis" (WIZ-04)
- `nios.html` running block `<header>` reads "Step 3: Run Analysis" (WIZ-04)
- Wizard step label sequence is visually consistent across all states: Step 1 Upload Backup → Step 2 Assign Members → Step 3 Run Analysis
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/20-migration-wizard-ux/20-02-SUMMARY.md`
</output>
